# TIP-86 Decision-threshold config lock - lift FaceMatch/Liveness thresholds to validated config (plumbing-only, effective 0.80 unchanged) - Planning Brief

Status: READY_FOR_BUILD v1.0 (Homeowner flipped 2026-07-09; Codex+GPT review-converged). v0.5 - Codex round-3 factual correction patched. **CORRECTED an earlier over-claim:** the decimal-scale trap (`"0.8"` vs `"0.80"` -> different proof hash) is NOT code-true - `EvidenceCanonicalization` formats via `GetDouble()->ToString("G")` (`:137-157`), erasing scale (`4.50->4.5`, `Tip65...:14-21`), so those spellings hash IDENTICALLY. Reworded: the prod rule is a GOVERNANCE VALUE-LOCK (accept only value `== 0.80m`; `0.75` => INVALID; spelling irrelevant), NOT byte-identity protection; the byte-identity DoD test still holds because the VALUE is unchanged. Also fixed: grounding line "Application only" (removed stale "or Contracts"); merged duplicate DoD prod-fixture bullets. Prior locks stand: OQ-1/2/3 (decimal / prod-explicit / plumbing-0.80-only); options in Application ONLY; NOT tenant-scoped (global per deployment, per-profile deferred TIP-70); deterministic field order; existing proof-hashes MUST NOT be edited; ctor blast-radius (test call-sites default 0.80); runbook calibration-governance line. Both reviewers: build-lockable after these. REVIEW-CONVERGED / BUILD-LOCKABLE; awaits Homeowner READY_FOR_BUILD flip. Repo: D:\Task\Remote Signing\TagEkyc (server), baseline origin/master 4bce858 (post TIP-83E-3). Grounded on the Codex recon (09/07). **Plumbing-only: the two hardcoded decision thresholds move to config with the SAME default 0.80m; the effective value + comparison + signed decision-basis shape are byte-unchanged.** The risky part is NOT config-sourcing - it is any actual VALUE change (proof-bound). This slice does NOT change the value, the assurance calc, or the proof shape.
Date: 2026-07-09

## 0. Grounding (Codex recon 09/07, file:line)
- **The consts:** `FaceMatchThreshold = 0.80m` (`VerificationEvidenceApplicationService.cs:20`), `LivenessThreshold = 0.80m` (`:21`). Comparison is `>=` for BOTH.
- **5 read sites (the ONLY sites to change):** FaceMatch pass/fail `basis.MatchScore.Value >= FaceMatchThreshold` (`:855`); FaceMatch proof-bound basis `thresholdApplied = FaceMatchThreshold` (`:1036`); Liveness submitted-threshold conflict `basis.ThresholdApplied.Value != LivenessThreshold` (`:1094`); Liveness pass/fail `>= LivenessThreshold` (`:1155`); Liveness proof-bound basis `thresholdApplied = LivenessThreshold` (`:1287`).
- **[PROOF-BOUND - the load-bearing redline] the threshold VALUE is committed into the signed proof.** Chain: `thresholdApplied` in the normalized decision-basis (`:1036`/`:1287`) -> payload hash (`:908` face / `:1213` liveness, canon labels `tip-70-face-match-decision-basis` `:909` / `tip-72-liveness-decision-basis` `:1214`) -> stored `PayloadHash` on `EvidenceResult` (`:389`/`:400`) -> manifest evidence refs `PayloadHash` (`VerificationCompletionApplicationService.cs:633-646`) -> proof signs `manifestHash` (`:220-235`) -> `resultHashPreimage` includes `signedManifestHash` (`Es256JwsEvidenceSignatureBuilder.cs:31-50`). **So a non-0.80 effective value CHANGES the signed artifact + breaks every proof-bound hash test.** Byte-stable ONLY when the effective value stays exactly `0.80m`.
- **Layer boundary:** `VerificationEvidenceApplicationService` is in **Application**, whose csproj references ONLY Contracts + Domain (`TagEkyc.Application.csproj:8`) - NOT Infrastructure. So the options class must live in **Application ONLY** (NOT Contracts, NOT Infrastructure - unlike RetentionOptions which is in Infrastructure). Do NOT change the dependency graph.
- **Prod-guard pattern to mirror:** the TIP-83E-3 retention raw-config guard (`Program.cs:206-238`) - `IsProduction()`-gated, reads raw config, throws exact stable codes, runs after signing/persistence/apikey.
- **Tests that pin 0.80m (stay green IFF default unchanged):** expected basis hashes hardcode `thresholdApplied = 0.80m` (`Tip05EvidenceApplicationTests.cs:1365`/`:1419`); Tip08 e2e expected payload hashes `thresholdApplied = 0.80m` (`Tip08TransactionBoundE2eProofTests.cs:502`/`:557`); below-threshold-coerces-to-review (`Tip05...:517`/`:708`); proof-bound manifest hash (`Tip06CompletionApplicationTests.cs:178-292`). **GAP: no exact-boundary test for score == 0.80m** (so a `>=`->`>` regression would NOT be caught) - add one FaceMatch + one Liveness boundary test.

## 1. Intent
Lift the two hardcoded decision thresholds to a validated, production-declared config (`DecisionThresholdOptions`) so the trial can calibrate them LATER without a code change - while keeping the effective value at `0.80m` and the signed decision-basis byte-identical in this slice. Production must EXPLICITLY declare the thresholds (a proof-bound, decision-affecting value should not ride a hidden default). The actual CALIBRATED value stays data/Legal-gated (calibration 5.2); this slice ships ONLY the plumbing + range validation + the current 0.80 behavior.

## 2. Scope - ALLOWED

### 2.1 `DecisionThresholdOptions` (in Application, NOT Infrastructure, NOT Contracts)
- New `DecisionThresholdOptions` with `SectionName = "TagEkyc:DecisionThresholds"` + two **[OQ-1 LOCKED] `decimal`** fields `FaceMatch` + `Liveness` (decimal NOT double - the consts are `0.80m` and `thresholdApplied` enters proof-bound canonicalization; binary float would risk serialization drift), **defaults BOTH = `0.80m`** (the current const values, byte-exact). **[R:P2-01] Place in Application** (server config, not an external contract surface; and `TagEkyc.Application.csproj:8` refs Contracts+Domain only so it stays in-layer) - NOT Contracts, NOT Infrastructure.
- **[R:first-reviewer] NOT tenant-scoped:** these thresholds are GLOBAL per server deployment/environment - there is NO tenant/client/profile key. Per-`VerificationProfile`/per-tenant threshold override is DEFERRED debt (TIP-70 `tip_70_planning_brief_v0_1.md:99` "single global server config"). Do NOT add a tenant/profile dimension.

### 2.2 Register/bind + production fail-closed guard (mirror retention)
- Register `.AddOptions<DecisionThresholdOptions>().Bind(section).Validate(range).ValidateOnStart()` in `Program.cs` (near the existing config, `:27-43`), running AFTER signing/persistence/apikey (+ retention) so it does not mask their `PROD_*` codes.
- Raw-config prod guard `ValidateDecisionThresholdConfiguration(builder)` (mirror the retention guard `Program.cs:206-238`), `IsProduction()`-gated, reads the raw config strings. **[R:P1-06 deterministic order] validate FaceMatch first, then Liveness; throw the FIRST exact code only.** Each threshold ABSENT => `PROD_THRESHOLD_FACE_MATCH_MISSING` / `PROD_THRESHOLD_LIVENESS_MISSING`; INVALID => `PROD_THRESHOLD_FACE_MATCH_INVALID` / `PROD_THRESHOLD_LIVENESS_INVALID`. Stable CODE only - no raw binder message, no value echoed. Non-Production => NO throw (0.80 defaults apply).
- **[R:latest - governance VALUE-lock, NOT a byte-identity rule (corrects an earlier premise)]** for THIS slice the production threshold MUST equal the currently-approved value **`0.80`** (i.e. parse to `0.80m`); any OTHER value (e.g. `0.75`) => `PROD_THRESHOLD_*_INVALID` - a calibrated value `!= 0.80` is data/Legal-gated (calibration 5.2), NOT settable via ops config here. **Correction:** an earlier draft claimed `"0.8"` vs `"0.80"` (decimal scale) would change the proof hash - that is NOT code-true. `EvidenceCanonicalization` formats numbers via `GetDouble()->ToString("G")` (`EvidenceCanonicalization.cs:137-157`), which ERASES decimal scale (`4.50->4.5`, tested `Tip65EvidenceCanonicalizationTests.cs:14-21`), so `0.8`/`0.80`/`0.800` all canonicalize + hash IDENTICALLY. So the guard checks the numeric VALUE (`== 0.80m`), NOT the spelling; the byte-identity DoD test still holds because the value is unchanged. The rule is a GOVERNANCE lock (no unapproved value silently enters prod), not a hash-protection.
- **[R:P1-05 two validation layers - do NOT conflate]** (i) the general Options range is `(0, 1]` (for the FUTURE calibration TIP); (ii) **the TIP-86 PRODUCTION guard accepts ONLY a value `== 0.80m`** for both - it does NOT allow an arbitrary in-range value like `0.75` now (that is a calibrated value = data/Legal-gated). Non-prod defaults remain `0.80m`.
- **[proof-bound note]** production `0.80` is byte-identical to today; ANY other value changes signed proofs (calibration, out of this slice).

### 2.3 Inject + read the resolved options at the 5 sites ONLY
- Inject the RESOLVED threshold values into `VerificationEvidenceApplicationService` via its constructor - as a plain Application/Domain POCO or two `decimal`s. **[self-check] Do NOT inject `IOptions<DecisionThresholdOptions>` into the Application service** - that would add a `Microsoft.Extensions.Options` package reference to `TagEkyc.Application` (csproj refs Contracts+Domain only, `:8`) and change the layer's dependency graph. Instead the COMPOSITION ROOT (API `Program.cs`) resolves `IOptions<DecisionThresholdOptions>.Value` and passes the plain POCO/decimals into the service registration. Replace ONLY the 5 const read sites (`:855`/`:1036`/`:1094`/`:1155`/`:1287`) with the injected values. Change NOTHING else.

### 2.4 Boundary tests (close the recon gap)
- Add ONE FaceMatch + ONE Liveness test at score EXACTLY `0.80m` asserting PASS (proves `>=` is preserved; would catch a `>=`->`>` regression). Existing `thresholdApplied = 0.80m` hash tests must stay green unchanged (proves the default is byte-exact).

## 3. STOP / NOT
- **The default MUST stay EXACTLY `0.80m` for both** - a different effective value changes the proof-bound payload/manifest/result hash chain (`:1036`->...->`Es256JwsEvidenceSignatureBuilder.cs:50`) and breaks every proof-bound hash test. This slice does NOT change the value.
- KEEP the `>=` comparison operator (do NOT switch to `>`).
- Do NOT touch: `CalculateAssuranceLevel` (`VerificationCompletionApplicationService.cs:586-602`), the canonicalization labels (`tip-70-face-match-decision-basis` `:909` / `tip-72-liveness-decision-basis` `:1214`), the normalized-basis shape, the signed proof/manifest shape, or the `VerificationDecision` record.
- Do NOT put the options class in Infrastructure or change the Application csproj dependency graph.
- Do NOT ship a new CALIBRATED value (data/Legal-gated, calibration 5.2); ship plumbing + validation + 0.80 only.
- No new migration; no persistence change; no proof/decision-basis LOGIC change beyond sourcing the same 0.80 from config.
- **[R:P1-08] Do NOT update the expected proof-bound hash values in existing tests.** If an existing `thresholdApplied = 0.80m` hash test changes, the build is WRONG (the effective value/serialization drifted) - fix the build, NEVER "fix" the test.
- NO tenant/profile/client dimension (global-per-deployment only; §2.1).

## 4. Definition of Done
- [ ] `DecisionThresholdOptions` (**Application only** - NOT Contracts/Infrastructure; section `TagEkyc:DecisionThresholds`, `decimal` FaceMatch+Liveness defaults `0.80m`).
- [ ] Registered + bound + range-validated (`(0,1]`); raw-config prod guard (after signing/persistence/apikey/retention): absent => `PROD_THRESHOLD_*_MISSING`; out-of-range/unparseable / value `!= 0.80m` (governance value-lock) => `PROD_THRESHOLD_*_INVALID` (exact codes, no binder-message/value leak); non-prod => no throw.
- [ ] The 5 read sites use the resolved options; NOTHING else changed in `VerificationEvidenceApplicationService` (diff scoped to those lines + the ctor).
- [ ] **[R:P3 ctor blast-radius] the service is `new`'d directly in many unit/integration tests** - all such direct constructions supply the default `0.80m` thresholds (a shared test helper OR a defaulted ctor param), with NO behavior drift; enumerate + update every call site so tests compile + stay green.
- [ ] **Effective 0.80 byte-exact:** all existing `thresholdApplied = 0.80m` proof-bound hash tests (`Tip05...:1365`/`:1419`, `Tip08...:502`/`:557`, `Tip06...`) stay GREEN unchanged.
- [ ] **[CRITICAL decimal-scale test]** a prod-config `"0.80"` path produces a payload hash BYTE-IDENTICAL to the default const-`0.80m` path (proves byte-exact serialized form).
- [ ] **[R:P1-07 negative test]** prod-config `"0.75"` (in-range but != the approved 0.80) => `PROD_THRESHOLD_*_INVALID`; `"1.5"`/`"0"`/`"-1"`/`"abc"` => INVALID; absent => `PROD_THRESHOLD_*_MISSING`. (Spellings that equal 0.80 - `"0.80"`/`"0.8"` - are VALID; the guard checks value `== 0.80m`, not spelling.)
- [ ] **Boundary tests added:** score == 0.80 => PASS for FaceMatch + Liveness (guards the `>=`).
- [ ] **Existing proof-bound hash tests kept UNCHANGED** (expected values NOT edited - drift = build wrong); prod-config test fixtures updated ONLY to ADD the `0.80` threshold declaration (a ripple of the new prod guard, like retention) - additive, non-masking; a broken signing+threshold config together => `PROD_SIGNING_*` still surfaces (not masked).
- [ ] `dotnet build` + `dotnet test` green; no migration; no persistence/proof-shape/assurance-calc change.

## 5. Review tier & attacks
Tier-1 (touches the proof-bound decision path - low behavior change but HIGH consequence if the signed-artifact shape/value drifts). Attacks: (a) does the default drift from exactly `0.80m` (breaks signed proofs + hash tests)? (b) does the comparison flip `>=`->`>` (boundary behavior change)? (c) does the change touch the normalized-basis shape / canon labels / assurance calc / proof-manifest shape? (d) is the options class wrongly placed in Infrastructure (layer-boundary break)? (e) does the prod guard leak a raw binder message or the value, or fail to throw the exact `PROD_THRESHOLD_*` code? (f) does a broken threshold config mask `PROD_SIGNING_*`/`PROD_DB_*`/etc (ordering)? (g) does the slice smuggle in a calibrated (non-0.80) value (data/Legal-gated)? (h) more than the 5 read sites changed?

## 6. Open questions - ALL LOCKED (both reviewers required these + the decimal rule closed before build)
- **OQ-1 [LOCKED]:** field type = `decimal` (NOT double) - proof-bound serialization must be byte-identical; binary float risks representation drift.
- **OQ-2 [LOCKED]:** production REQUIRES explicit `TagEkyc:DecisionThresholds:FaceMatch` + `:Liveness` (a proof-bound decision parameter must not ride a hidden default); absent => exact `PROD_THRESHOLD_*_MISSING`. Non-prod may use the `0.80m` defaults.
- **OQ-3 [LOCKED]:** TIP-86 ships plumbing + validation + effective `0.80` ONLY. Any calibrated value `!= 0.80`, or any change to the `thresholdApplied` proof-bound basis/canonicalization, requires a separate Homeowner/Legal calibration decision (calibration 5.2).
- **Value-lock rule [LOCKED, §2.2]:** prod accepts ONLY a value `== 0.80m` (any spelling that parses to 0.80 is fine); any other value (e.g. `0.75`) => INVALID. Governance lock (no unapproved value in prod), NOT a byte-identity rule - the canonicalization erases decimal scale (`GetDouble->ToString("G")`), so `0.8`/`0.80` hash identically.

## 7. Companion / boundary
- **Calibration 5.2** (the actual FaceMatch/Liveness threshold VALUES from consented samples -> APCER/BPCER -> a calibrated number) is data/Legal-gated; TIP-86 gives it a config seam so the calibrated value lands WITHOUT a code change (and without touching the signed-proof plumbing).
- **Changing `thresholdApplied` in the proof-bound basis** (e.g. removing it, or a different canonicalization) is a proof-contract change - NOT this slice; needs its own governance.
- **[R:P2-02] Runbook line:** changing a threshold changes signed evidence/proof outputs and MUST be governed as CALIBRATION (Legal/DPO), not an ops config tweak. (If a runbook mentions the `TagEkyc:DecisionThresholds` config, it carries this warning.)
- **Per-tenant/per-profile threshold policy** = deferred debt (TIP-70 `:99`); TIP-86 is single global server config.
