# A3 macro-wave W3 admission outcomes — preflight v1

Status: **EXECUTION AUTHORIZED / NO ROW CLOSED**. Incoming authority-open census is **48 = 47 normative + 1 seam**. A3 remains HOLD.

## Exact scope

W3 contains exactly seven rows and no others: P01, P06, P07, P08, P11, P14 and P16. The authoritative scenario inventory is `a3_macro_wave_w3_execution_matrix_v1.tsv`.

Execution is horizontal by four shared harnesses, not by row:

1. **W3-A AUTH_ADMISSION** — one production A1 signed-HTTP/PostgreSQL arrangement covers the denial matrix and the unread-body positive control.
2. **W3-B PRE_BODY_LIMITS** — one metadata/expiry arrangement covers P06 declared-size reachability and P07's five effective-expiry sources.
3. **W3-C CAPACITY** — one held-slot arrangement covers denial, release and the next admitted call.
4. **W3-D BROKER_PRE_BODY_FINALS** — one PostgreSQL/Kestrel/TLS/production-Agent arrangement covers O11/O14/O16 and their neighboring controls.

P06 is a pre-declared product gap, not a reason to stop the other six rows. Current production receives the claimed length but not the ratified per-class maximum at A3 admission, so declared excess cannot be rejected before capacity/broker/body. The existing actual-excess R2 proof remains component evidence. Synthetic `Final(O06)` is prohibited as producer proof.

## Resource-minimizing execution rule

Before the first test run, finish all product/test edits and the complete guard inventory. Then execute only these phases:

1. one build of the W3 candidate;
2. one current-byte baseline containing every W3 positive scenario that can execute;
3. mutations by the shared guard IDs in the TSV, running the complete affected family plus any ratified sentinel that depends on the mutated source;
4. exact byte restoration of every mutated source;
5. one rebuild;
6. one joined restored W3 gate;
7. one affected-source ratified sentinel;
8. one whole-repository TRX census, manifest/drift freeze and one review packet.

No build/restart cycle is created merely because a second RowId exists. P06 may end as a named product blocker while the remaining families continue. No full suite, stage, commit, push or production activation is authorized.

## Existing evidence reuse

- P01 reuses the production A1 signed-HTTP boundary; the new work is the current-byte joined denial/Agent disposition and guard discrimination, not a second authenticator fixture.
- P07 reuses the accepted strict comparator correction. New work is limited to making each member of the five-way `LEAST(...)` the unique minimum and mutating each member/branch independently.
- P08 reuses the real capacity primitive and its load-bearing denial guard. New work is the public Kestrel/Agent and custody/retry join.
- P11/P14/P16 reuse the existing shared broker-claim Kestrel arrangement. New work is only the missing row dimensions and current-byte guard repin; the harness is not duplicated.

## Stop conditions

Stop the wave only for a regression in a ratified row, an undeclared product-route change, a live mutant after restoration, an unclassified failed TRX, or an authority conflict. A named row blocker does not stop independent W3 families.

The canonical reconciliation and candidate manifest are regenerated once, at end-of-wave. During execution every run is appended to the macro-wave scratch ledger with TRX hash, source hash, guard, classification and restore state.
