# A3 macro-wave W2 — successor review packet v2

**Status:** CANDIDATE — independent review required  
**Date:** 2026-09-20  
**Scope:** whole W2 wave; successor to the over-broad v1 disposition  
**Disposition:** P03/P12/P13 candidate; P04/P05/P29–P36/BP17 open  
**Non-claims:** not A3 PASS; no full suite, stage, commit, push, landing or production activation

## 1. Corrected whole-wave disposition

| Rows | Disposition | Exact reason |
| --- | --- | --- |
| P03/O03 | **CANDIDATE_PENDING_RATIFICATION** | Real caller/artifact ownership denial produces O03 before capacity/body. Kestrel/TLS→production Agent, exact 403 shape, zero body read, unchanged residue and discriminating O03→O02 mutation are joined. |
| P12/O12 | **CANDIDATE_PENDING_RATIFICATION** | Real PostgreSQL claim-state path produces typed O12; current Kestrel/Agent path, concurrent-winner residue and discriminating cause mutation are joined. |
| P13/O13 | **CANDIDATE_PENDING_RATIFICATION** | Real current-authority denial produces O13; Kestrel/Agent path, zero provider/body work, exact residue and discriminating projection mutation are joined. |
| P04/O04 | OPEN | W2 proves fresh-request transport-invalid only. Existing-R1 pre-start terminalization/fenced cleanup is absent. |
| P05/O05 | OPEN | W2 proves unsupported canonical class only. Active-key/provider capability-unavailable, Evaluating shell/budget preservation and restored-capability retry are absent. |
| P29–P36/O29–O36 | OPEN ×8 | Exact component outcomes and worker shell are load-bearing, but `IRawExportAssemblyWorkSource` has no production implementation/registration. The shipping work-source→worker→real orchestrator→durable RecordAsync join is absent. |
| BP17 provider-unknown | OPEN | Complete durable inspection→O21, incomplete/unknown→O18 and no invented O19 remain separate rather than one discriminating matrix. |

No v1 component evidence is discarded; it is retained at its actual scope. Only the row-closure claim is narrowed.

## 2. Whole-repository failed-run census

The v1 verifier saw only the seven TRX under `tests/.../TestResults/a3-w2`. The successor enumerates **every `a3-w2-*.trx` in the whole Server and Agent repositories**, including ignored root `TestResults/a3-w2` paths.

The complete inventory contains thirteen TRX and six failed runs:

- 2 `EVIDENCE_RED`: the current shared guard matrix and worker-shell caller mutation;
- 3 `SUPERSEDED_RED`: wrong-flag assembly predecessor, early mixed 26-case baseline, and the first O12 residue retry;
- 1 `EXCLUDED_DIAGNOSTIC`: MinIO `Access Denied` before the assembly target assertion;
- 0 unclassified.

The early 26-case baseline's four failures are individually explained: P33 mapping had not been corrected; O12 used invalid multi-command prepared SQL; the worker asserted exactly one poll instead of at least one; and the NPS evidence-count expectation predated the expanded setup. Its successor is the current 19/19 baseline plus the 18/18 current-source sentinel. The O12 residue predecessor is succeeded by a focused 1/1 and both final horizontal gates.

All thirteen TRX are named in `a3_macro_wave_w2_trx_inventory_v2.tsv`, referenced by the successor packet/census and therefore forced into the candidate manifest even when ignored by Git.

## 3. Current-byte evidence

The accepted mechanics remain:

```text
current baseline                19/19 PASS
shared row-guard build           9 PASS / 10 FAIL
worker-shell caller mutation     0/1 RED
final restored joined gate      20/20 PASS
```

The shared mutation proves only guards reached by its scenarios. It supports exact closure for P03/P12/P13, while remaining component evidence for P04/P05/P29–P36.

## 4. Open gaps not over-closed

`RawExportAssemblyHostedService` is real product code and the caller mutation is valid, but the worker test supplies a recording work source and recording orchestrator. Production readiness intentionally fails with `PROD_RAW_EXPORT_ASSEMBLY_WORK_SOURCE_UNAVAILABLE` because no production work-source implementation is registered. Therefore the shell cannot be presented as the shipping workflow.

Likewise, the fresh P04 and unsupported-class P05 producers are real, but they do not cover the additional stateful branches required by their rows. These ten rows return to `NOT IMPLEMENTED` with exact exit conditions rather than losing their component evidence.

## 5. Current-byte ratified sentinel

W2 changed **15 Server product-source paths relative to the W1 manifest**, not 69; the larger number is the accumulated dirty worktree. Some W2 changes touch the A3 façade/migration used by ratified rows, so one focused current-byte sentinel was run.

`a3-w2-current-source-ratified-sentinel.trx` covers:

- all nine Kestrel/TLS→Agent publication/replay scenarios used by BP11 and P17–P22;
- BP16 durable-ciphertext verifier;
- both BP17 ciphertext controls;
- both BP18 intent/stage orders;
- all four BP21 provider-evidence states.

It passed **18/18**, zero skip, on current bytes. The expensive ten-cut process-kill matrix was not rerun because none of the W2 changes altered its child-process harness; this sentinel targets the affected production surface.

## 6. Homeowner scope authority

The earlier v2 authority correction correctly rejected builder inference from Planning Brief §10.2. The missing scope signature is now explicit in `a3_macro_wave_w2_homeowner_scope_decision_v1.md`: after the grouped plan/audit, the Homeowner instructed **“Làm đi”** and subsequently rejected parking while product obligations remain. The selected scope is `WIRE_PRODUCT`.

That decision authorizes implementation only. It does not ratify rows or convert fixture/component proof into shipping proof.

## 7. Requested one-wave disposition

Ratify **P03, P12 and P13 together**. Keep the other eleven W2 rows open exactly as listed. Before review the authority census remains **50 normative + 1 seam = 51**. Ratifying the three candidates changes it to **47 normative + 1 seam = 48**. A3 remains HOLD.

## 8. Machine counters

```text
manifest_files=1921
successor_delta_added=14 successor_delta_modified=6 successor_delta_deleted=0
trx_total=13 whole_repo_w2_trx=13
failed_runs_total=6
evidence_red=2 superseded_red=3 excluded_diagnostic=1
unclassified_failed_runs=0 outside_name_family_failed=0
mutants=98 live_mutant_matches=0
official_open_rows=51 assigned_once=51 duplicate=0 unassigned=0 extra=0
staged_server=0 conflicted_server=0
staged_agent=0 conflicted_agent=0
```
