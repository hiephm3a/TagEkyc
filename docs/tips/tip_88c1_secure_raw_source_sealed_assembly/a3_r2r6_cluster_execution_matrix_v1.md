# A3 R2–R6 cluster execution matrix v1 — Phase A inventory and edits

This is an execution inventory, not a review packet or an A3 readiness claim. The pinned incoming bytes are reconciliation `6783C5D6D7FADC5E1C0DB85B70E4FF05BBB7B829C9B7D8A0A7E1502A827D8A07`, open matrix `B6F78A62C4843A470E340ECCD27B0C388B7D4CECF2C4592ED4D6EA5215A6E0FE`, and mutant registry `F99A632426F069F9D0AF1EB8DFA56585DB59C508B47DCF9104BE5070DD60C23A`; these are historical incoming pins, not current-file SHAs. The preflight matrix has **20 historical R2_R6 rows**. Ratification chronology is E01 66 open → BP18 65 → BP21 provider-evidence 64 → BP17 ciphertext-cause 63 official open. The prior CC figure 64 preceded the BP17 ratification and is superseded. BP16 and BP11 are two further builder candidates; the current ledger mechanically counts **60 normative + 1 seam = 61 candidate open**. BP16's non-null `plaintextConsumer` guard was dynamically mutated and restored; BP21's four-case mutation repeat is complete. No other R2_R6 row is closed by component scenarios. The later cluster correction supersedes row-count handoff thresholds: final cluster review still requires every scenario disposition and a joined restored gate.

## Guard and scenario inventory

Each guard below is a production decision, not a proposed new pipeline. One minimum mutation is planned per *unique guard*; existing valid/corrupt/clock/kill/race controls may map to several rows. Transient mutation of the verifier's `plaintextConsumer:null` call is specifically directed by the current Homeowner instruction; the verifier is otherwise listed read-only in the older v0.6 source inventory and must be restored to its pinned SHA after that isolated test. A persistent verifier change is **not** authorized.

| Guard | Production decision and principal source | Shared durable pre-state | Recovery owner | Candidate crash/negative boundary | Fixture/infrastructure |
| --- | --- | --- | --- | --- | --- |
| G-V | CP08 exact object ID → R2 verifier's exact S3 read and null plaintext consumer (`CaptureRuntimeSourcePipeline`, `RawExportR2CompletionVerifier`) | committed R1; object present/pending verification, no raw stream | Reconciler | after PUT/before verification; non-null consumer must be observable | disposable PostgreSQL DB, one MinIO namespace, role-qualified scopes, synthetic KEK |
| G-U | R2 input observation and provider-unknown precedence (`CaptureRuntimeRawIngressBodyPipeline`, R2 writer) | incomplete/unknown PUT versus complete object; no fabricated clean EOF | Writer then Reconciler | interrupted read, unknown provider response, complete ciphertext | controlled reader/provider fault; no canned business result |
| G-T | terminal intent/cause/finalization fence (TI01/TI02 and pipeline) | exact attempt with O19/O20 intent; key/object unsettled or settled | Reconciler then Lifecycle | after intent commit; before object/key settlement; before finalization | real PostgreSQL/MinIO with independent observer |
| G-N | NPS01 positive no-provider-start predicates and lease clock | R1 committed, no provider started versus four provider evidence states | Reconciler | after R1 commit/before prepare; observed lease expiry | PostgreSQL clock, role connection; provider call counters |
| G-E | RE01 same-owner reentry and original business horizons | old attempt safely settled; live original H; current head | broker B-C/Reconciler | after old lease, before/after H; race with R3 or TI01 | two PostgreSQL connections, session-prefix lock |
| G-S | R3 staging/observed CAS and TI intent exclusion | VerifiedCompleted, no publication; exact attempt/object revisions | Reconciler | after verify/before R3; concurrent TI01 | PostgreSQL lock and observer; no fake stage result |
| G-C | R4 commit from fresh CP08 tuple | Staged; no Available publication | Reconciler | after R3/before R4; stale CAS | PostgreSQL observer, existing service |
| G-P | R5 publish before Available projection and O17 replay | Committed/staged versus published Available head | Reconciler/broker replay | after R4/before R5; after R5/before response | PostgreSQL, HTTP route, independent observer |
| G-L | R6 exact cleanup and receipt-loss continuation | Available with pending cleanup item and exact object/key | Lifecycle/Reconciler | after publication/before cleanup; after provider delete/before receipt | exact MinIO key/object operations, process-kill child |
| G-M | stage/result enum and impossible-shape projection | each real writer/verifier/stage/read/complete/finalize result | pipeline/broker mapper | unknown enum/shape; no fabricated O20 or Available | production services, bounded synthetic prerequisites only |
| G-H | public HTTP/Agent result shape and pre-body behavior | durable Available, terminal intent/final or complete-ciphertext residue | API broker/Agent client | lost response, retry, replay | Kestrel/PostgreSQL where required; TestServer only when explicitly scoped |

## Complete 20-row mapping

`B0` = R1 committed/pre-provider; `B1` = PUT outcome unknown; `B2` = object present/pre-verifier; `B3` = verified/pre-R3; `B4` = R3 staged/pre-R4; `B5` = R4 committed/pre-R5; `B6` = R5 published/pre-response; `B7` = Available/pending R6; `T0–T3` = terminal intent before/through settlement/finalization. Each process-kill case will be selected by unique `(boundary, durable pre-state, recovery owner)`, not row count. Child process and DB/object namespace must be isolated; PostgreSQL/MinIO fixture reuse is allowed only when namespace isolation and cleanup are proved.

| Historical matrix row | Current disposition | Guard(s) | Scenario / durable pre-state | Recovery owner / boundary |
| --- | --- | --- | --- | --- |
| BP11 AvailableOnlyAfterPublicationCommit | Builder candidate; historical narrow RED and restored GREEN, current HTTPS 4/4 includes publication control; independent ratification pending | G-P,G-H | verified→staged→committed before publish; later published control | Reconciler/broker; B3–B6 |
| BP12 CleanupSurvivesHttpReceiptLoss | Open | G-L,G-H | Available with pending cleanup and lost HTTP receipt | Lifecycle/Reconciler; B6–B7 |
| BP16 VerifierRunsOnDurableCiphertextNotRequestBuffer | Builder candidate; exact-object and non-null plaintext-consumer mutants RED; independent ratification pending | G-V | exact stored ciphertext; raw request disposed; valid/corrupt controls | Reconciler; B2 |
| BP17 ProviderUnknownDoesNotInventInputMismatch | Open; semantic projection conflict recorded | G-U,G-M | unknown PUT/provider versus complete ciphertext | Writer/Reconciler; B1–B2 |
| BP17 CiphertextFailureIsNotProducerMismatch | Ratified by current cluster directive | G-V,G-T | same-length persisted ciphertext tamper versus valid control | Reconciler/Lifecycle; B2,T0–T3 |
| BP18 StageCannotOvertakeTerminalIntent | Ratified regression anchor | G-T,G-S | two actual TI01/R3 lock orders | Reconciler; B3–B4,T0 |
| BP19 AllStageEnumMembersHaveExactProjection | Open; unknown enum semantics blocker recorded | G-M | all live stage results, malformed and unknown enum controls | pipeline; B1–B7,T0–T3 |
| BP20 TransientO18SettlesThenSameOwnerRetryReachesAvailable | Open | G-E,G-V,G-S,G-C,G-P | old settled attempt, new attempt after original lease before H | broker/Reconciler; B0–B6 |
| BP20 ReentryCannotRaceStageOrTerminalIntent | Open | G-E,G-S,G-T | current head contested by B-C, R3 or TI01 | broker/Reconciler; B3–B4,T0 |
| BP20 TerminalOrDifferentOwnerCannotReenter | Open | G-E,G-T | O06/O19/O20 final or changed owner/binding | broker/Reconciler; T3 |
| BP20 ExpiredTransientAttemptFinalizesRecaptureWithoutHistoryRewrite | Open | G-E,G-T | old settled attempt at/beyond original H | Reconciler/Lifecycle; T0–T3 |
| BP21 LostR1ResponseBeforeKeyRecoversWithoutMaterial | Open | G-N,G-E,G-V,G-P | R1 commit, no key/provider; NPS01→RE01→Available | Reconciler; B0–B6 |
| BP21 ProviderEvidenceCannotMasqueradeAsNoStart | Ratified anchor; four-case mutation repeat complete (three RED, object-custody GREEN) | G-N | provider-unknown/object-custody/active-key/preparation | Reconciler; B0 |
| P17 O17 AlreadyAvailable | Open | G-P,G-H | durable published descriptor replay, no body/key/object | broker/API/Agent; B6 |
| P18 O18 TemporarilyUnavailable | Open | G-U,G-T,G-L,G-H | transient cleanup/read failure, next retry re-evaluates | Reconciler/API/Agent; B1,T0–T2,B7 |
| P19 O19 ContentCommitmentMismatch | Open | G-U,G-T,G-H | clean complete producer-body mismatch | Writer/Lifecycle/API/Agent; T0–T3 |
| P20 O20 RecaptureRequired | Open | G-T,G-H | exact server terminal plus distinct local lost-buffer no-call | Lifecycle/API/Agent; T0–T3 |
| P21 O21 ResumePending | Open | G-U,G-V,G-S,G-C,G-P,G-H | complete durable ciphertext, continuation after killed request | Reconciler/API/Agent; B1–B5 |
| P22 O22 Available | Open | G-V,G-S,G-C,G-P,G-L,G-H | R2→R5 Available then R6 cleanup and O17 replay | Reconciler/Lifecycle/API/Agent; B2–B7 |
| P23 O23 EncryptionFailed (no egress) | Open | G-U,G-T,G-H | actual encryption failure; safe settlement, O18/O20 | Writer/Reconciler/API; B1,T0–T3 |

Phase B may correct only product defects inside the already granted A3 file set. BP17 provider-unknown and BP19 unknown-stage semantics remain recorded blockers; they do **not** prevent work on independent guards. No row closes from this matrix. No full suite, stage, commit, push, A4 or production activation.

## Phase C test arrangement and gap accounting

The cluster's positive controls are grouped by setup, not by ledger row. Existing tests remain intact as identity-preserving sentinels. The new `R2R6_ClusterProcessKillMatrixUsesSharedInfrastructureAndIsolatedSources` runs full-R2 cuts 0–5 followed by R6 cleanup cuts 0–3 with one MinIO instance and one compiled child probe; each cut receives a separate disposable database and source/object identity. It emits a result line per cut into the one TRX. This groups the B1–B7 boundaries and maps to BP12/BP16/P21/P22 *components*, without upgrading those whole rows merely because the matrix passes. Existing per-cut tests remain as historical sentinels.

`VerifierRunsOnDurableCiphertextNotRequestBuffer` now includes an explicit negative observation for a plaintext-consumer probe after production verification. The isolated G-V mutant will attach a non-null consumer at the verifier call site, set only this non-plaintext marker on invocation, and must fail the new observation. No plaintext bytes are retained or emitted by the probe. The test restores the ambient marker in `finally`.

The existing `ActivatedHttpRoute_UsesBrokerAndBodyPipelineToPublishBothRawClasses` is extended in both raw classes with a same-idempotency replay after the first R5 publication. A read-counting replay stream starts at position zero; public O17 must preserve the source descriptor while making zero body reads and adding zero key/object rows. This is a shared G-P/G-H component for P17/P22 and is not an exact pre-R5 BP11 proof. Its use of a fixture authenticator is explicitly post-authentication scope only.

The joined focused filter will also run `CiphertextFailureIsNotProducerMismatch` (both variants), `StageCannotOvertakeTerminalIntent` (both variants), and all four `ProviderEvidenceCannotMasqueradeAsNoStart` evidence variants. BP21's existing predicate mutant must be rerun against all four, reporting red and green variants separately. Missing BP17 provider-unknown and BP19 unknown-enum semantics stay classified; no canned stage result will close either. BP11/P17 still need an exact public before/after-R5 projection; BP12 still needs lost HTTP receipt joined to independent cleanup; P21 still needs its public outcome/Agent shape; P22 still needs the full public success/replay join. The shared kill matrix is a component for these, not a substitute for the missing assertion dimensions.

## Internal cluster checkpoint — no review packet, no new row disposition

The final current test source SHA-256 is `0FAD4CF24D7E3194C7D3C286363E38E6C0EBE8D634185294F5E15291EA6CF4D2`. The one shared-harness full-R2/R6 process-kill test emitted the following matrix in its TRX output, each after an actual child `Process.Kill` and a new child process with its own PID. Each helper established an isolated PostgreSQL database/source/object identity; all ten used the same MinIO server and compiled probe in the parent test.

| Boundary | Durable pre-state checked in helper | Recovery owner | Mapped proof component | Result |
| --- | --- | --- | --- | --- |
| full-R2 cut 0 | `PutOutcomeUnknown`, `Reserved` | Reconciler | BP16, P21/P22 | Available; one PUT/object/publication |
| full-R2 cut 1 | `ObjectPresentPendingVerification`, `Reserved` | Reconciler | BP16, P21/P22 | Available; one PUT/object/publication |
| full-R2 cut 2 | `VerifiedCompleted`, `Reserved` | Reconciler | BP11/P21/P22 | Available; one PUT/object/publication |
| full-R2 cut 3 | `VerifiedCompleted`, `Staged` | Reconciler | BP11/P21/P22 | Available; one PUT/object/publication |
| full-R2 cut 4 | `VerifiedCompleted`, `Staged` | Reconciler | BP11/P21/P22 | Available; one PUT/object/publication |
| full-R2 cut 5 | `VerifiedCompleted`, `Available` | Reconciler | BP11/P22 | Available; one PUT/object/publication |
| R6 cleanup cut 0–3 | Available publication plus durable pending cleanup item(s) | Lifecycle/Reconciler | BP12/P22 | cleanup Completed; winner object/key unchanged |

The first focused 10/10 TRX was `tests/TagEkyc.IntegrationTests/TestResults/a3-r2r6-cluster-baseline-focused.trx`, SHA-256 `1FBA96EE7155F4D56D6F78590CBF48B6FC13BA11CCE68F93B624AFCF3F7AC375`; it preceded the later HTTP replay test-source extension and is historical, not the final candidate run. The separate first HTTP replay 2/2 TRX was `a3-r2r6-http-replay-baseline.trx`, SHA-256 `E4E26092B84D08EAC2EA23E5BEEB1364801FF561031046CE657AE7040C0A79EC`.

The final-source isolated mutations were:

| Guard | Exact mutant source SHA-256 | RED TRX SHA-256 and counters | Target observation |
| --- | --- | --- | --- |
| G-V: replace verifier `plaintextConsumer:null` with marker-only non-null consumer | `7EED7480906F4779E19D0AAA402F1286ED28F2EB4CE5DF084D1106CD83390B1A` | `a3-r2r6-bp16-consumer-final-source-red.trx` `90D030F61BF771C0F921BB23DA50BB3D28EB1737BCDFBA648AE3112E80D6F399`, 0/1 | `Assert.Null` failed: actual marker `1` |
| G-N: NPS01 last `AND NOT EXISTS(provisional_objects)` → `OR NOT EXISTS` | `C961C389123F9FFD894CE584AC89A6426D6C82795B2DE7A7821175630C1CB1C1` | `a3-r2r6-bp21-four-evidence-final-source-red.trx` `393D382B439CA26F55D4E5D0DF6D8AB1530819C0D61DF1FE236F397A6A7001F2`, 1 pass / 3 fail | preparation, provider-unknown, active-key RED at NPS01 with independent append-only guard; object-custody GREEN |
| G-H: make final broker-result branch read one body byte | `3F54616B5ED7916368AAD5C495A531CCEB62CDE0CA904E64658433B00E99FA44` | `a3-r2r6-replay-body-read-final-source-red.trx` `9DC6A612A2B8255930A1FFD07EF64F8CBC6B99158075871C36020BF925125FFA`, 0/2 | both raw classes fail zero-read assertion on replay |

All three product files were restored byte-exact: verifier `58AF7E6639382DDF292F080C6E32868F26B83F78ABB86BEF74538391FD4E62A6`, A3 migration `91DD833A50E834145D34501F0C793D987A5A34C31CC011402D7DEAF7A485552A`, admission `87F6741623601E1B6150F7DB025BEC5A37A6D0ADC1650063838CD3BA14D55A63`. After rebuild, the joined current-source `a3-r2r6-cluster-restored-joined.trx` SHA-256 `867A92F335CBE3BB002797FB96A0B33CE5057CA16061436B4052A0ECD87F1C9D` is **12/12**, 0 fail/skip: BP16, BP17 ciphertext 2, BP18 race 2, BP21 evidence 4, HTTP Available/replay 2, and the ten-cut shared kill matrix 1. This is not a full regression. BP16's dynamic null-consumer gap is now directly mutation-sensitive as a builder candidate; P17/P21/P22 and the other open rows remain open pending their missing dimensions. This historical joined run is not a final cluster gate.

The next shared G-P/G-H component uses a separate new test file `Tip88C1C6BA3R2R6ClusterHttpTests.cs`, SHA-256 `06D38BBC780AF3240A4840A927BF414AE950DFDF50448B2CEAB13A1DA3629FCF`. A test-only PostgreSQL trigger pauses only the R5 `Available` update on an advisory lock, without replacing R2/R3/R4/R5 or response logic. The actual TestServer HTTP request reaches committed publication; while R5 waits, the independent observer sees `Committed`/not-Available and the response task remains unfinished. Releasing the lock yields public O22 and one Available publication. Baseline `a3-r2r6-bp11-http-r5-baseline.trx` SHA-256 `4E255399E054E4C4E44BF4217DD05B6C125BBE0A7799CCE1C6609FD900B49111` was 1/1. The isolated G-P projection mutant accepted `PublicationState=Committed` as Available, source SHA-256 `9D1C016FA2E378D6CB6A1FD7BFFE2942AC307DBD2B040B6F16AFF4C53C9D0053`; `a3-r2r6-bp11-early-available-red.trx` SHA-256 `774144B17E62E0AB4B3A65848354F57BDC041B08F3F2048E6E9DEDA985A281B2` was 0/1 at `Assert.False(responseTask.IsCompleted)` (`Actual: True`). The pipeline restored byte-exact to `5D9A2B7E161A700F61793A9E1E2F96562DB8FBF04EB2803B0A9989CE1F2F80AD`; rebuilt `a3-r2r6-bp11-http-r5-restored.trx` SHA-256 `8673F3C841A3D4992F0D2892D3DB3C57F66BC5620170BEE42C3A0859DFC6551F` was 1/1. This proves the response-before-R5 negative property on the TestServer post-authentication path; it does not claim real Kestrel transport/authentication or close BP11 by itself.

BP11's successor uses real loopback Kestrel and `HttpClient` on test-source SHA-256 `822EA6AC480ACDE9311EC8AEB77F83FDAF00F53EEB0AC8832AA267C1A3E25973`. The same database-local scheduling trigger and product path establish the exact Committed-but-not-Available phase while the public response is pending. `a3-r2r6-bp11-kestrel-r5-baseline.trx` SHA-256 `54DCF90E13B4EDB81AE6C520414C7EE668619801B976BD287C9A6B6578851ED1` was 1/1; same isolated projection mutant makes `a3-r2r6-bp11-kestrel-r5-red.trx` SHA-256 `8CE3324D0ACA2102913BB41BE32338F99F57A54EF98D1A87A60D50097E9D1674` 0/1 at the premature-response assertion; pipeline restored byte-exact and rebuilt `a3-r2r6-bp11-kestrel-r5-restored.trx` SHA-256 `4AE37013F70A2E64D71A740D25BE1D0CEFEC72806B1B06F48E5F2197050ADA3C` is 1/1. Fixture authentication supplies only a caller premise; the row claims no auth proof. BP11 is a builder candidate, not ratified; candidate census 60 normative + 1 seam = 61 open. No cluster handoff occurs until every scenario has a disposition and the joined restored gate is complete.

On final current test SHA-256 `F436E06801176E9F782912A03FFEB04C93C8B5D7E8FA8CAF6D8571AAE87CC0F9`, the same Kestrel scenario also observes O17 replay and capacity release. `Expect: 100-continue` distinguishes replay; a transparent server-side wrapper is confirmed attached once and counts zero `Request.Body` reads. The HTTP response retains the source ID, key/object counts remain equal, and a same-producer capacity lease can be reacquired. Baseline `a3-r2r6-bp11-p17-kestrel-final-baseline.trx` SHA-256 `F4519594F07AEE7453A70BD1DC51DE9CCA197E4E56BEA90512387DC22E5D234B` was 1/1. G-H final-branch forced-read mutant `3F54616B5ED7916368AAD5C495A531CCEB62CDE0CA904E64658433B00E99FA44` gave `a3-r2r6-p17-kestrel-server-read-red.trx` SHA-256 `1B4F7542DE5CD4FE7B70B234C1322550750D06EE6A80BA4B22F6C20FEA115A39` 0/1 at `Expected 0 / Actual 1`. G-P Committed→Available mutant `9D1C016FA2E378D6CB6A1FD7BFFE2942AC307DBD2B040B6F16AFF4C53C9D0053` gave `a3-r2r6-bp11-kestrel-final-source-red.trx` SHA-256 `57DE71AAFE6C362BA9E9498A8F23B530F3FDBD0DEF5AD2A83AC7BC3D238984C0` 0/1 at the premature-response assertion. Byte-restored rebuilt `a3-r2r6-bp11-p17-kestrel-final-restored.trx` SHA-256 `9E1CCC04F55284DC836FF70DF30B4949B64DF6AE4DFD1DC13A063967A3184441` was 1/1. P17 remains open for the production Agent-client join; no second row flip. The first attempt to count the **client** stream rather than server request body was excluded as a test-observation error, not a product RED.

## Cluster-atomic correction: transport scenario matrix (completed before further scenario work)

The four completed groups are frozen: ten-cut process-kill, BP16 plaintext-consumer mutation, four-case NPS mutation, and BP17/BP18/BP21 ratified sentinels. They rerun only in the final joined gate or on relevant product-source change. All Agent rows below require the **production** `CaptureRuntimeHttpClient`; a fake authenticator may establish the caller prerequisite but never proves authentication. TestServer proof is explicitly in-process and cannot substitute for Kestrel/TLS/`Expect`. A provider/response double may schedule or seed a pre-state but cannot supply the business result under test.

| Scenario | Server pre-state | HTTP host | TLS / auth prerequisite | Agent/client | Server body-read expectation | Expected response | Mapped rows | Production mutation guard | Current disposition |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| TH0 shared harness positive control | no business source/ingress | Kestrel loopback | installed .NET development certificate, real TLS handshake; no production-auth claim | real `HttpClient` health probe PASS; production Agent client transport smoke uses synthetic admission only to exercise the shared TLS handler | none for health; transport smoke makes no business body-read claim | 200 health response; synthetic O17 only for Agent transport smoke, no business closure | all TH1–TH10 | TLS harness only, no business mutation | TLS health PASS 1/1; Agent smoke PASS in Phase C, transport-only |
| TH1 publication visibility | R4 `Committed`, R5 `Available` transaction paused by DB lock | Kestrel; TestServer comparison already measured | TH0; fixture auth only | production Agent first send and replay | first body exactly once; replay zero body reads | no O22 before R5 commit; then 200/O22 exact descriptor, followed by O17 replay | BP11, P22 | G-P `Committed→Available` | component PASS over real Kestrel/TLS and Agent on same source; final-current-byte mutation/restored gate OPEN |
| TH2 published replay | R5 Available + immutable source/alias; same signed identity | Kestrel | TH0; fixture auth only | production Agent client | zero server reads despite `Expect: 100-continue` | 200/O17, original descriptor; no key/object, capacity released | P17, P22, BP12 replay control | G-H final-before-body guard; G-P publication projection | HTTPS/Agent branch PASS in focused Phase C; not a restored joined gate |
| TH3 preserved ciphertext / request lost | R2 completed durable object at B1–B5; first response absent | Kestrel | TH0; fixture auth only | production Agent client after reconnect | no body on continuation/replay | 202/O21 until R5 then 200/O22 | P21, BP16, P22 | G-V durable ciphertext; G-H pre-body classification | same-source canceled HTTPS request after R4 Committed → O21 bodyless retry → worker Available → O17 component PASS; separate same-source Process.Kill/O22 join OPEN |
| TH4 transient failure | unknown PUT or cleanup-incomplete attempt, terminal intent/operational settlement | Kestrel | TH0; fixture auth only | production Agent client retry | first ingress may read once; retry only per durable owner/lease | 503/O18, then reevaluate, never false terminal | P18, BP17 provider-unknown, BP20 transient | G-U unknown-vs-absence; G-T intent; G-E reentry | actual AEAD failure/O18 → positive object absence → same-lease O16 bodyless reevaluation component PASS; provider-unknown semantics BLOCKED, terminal/expiry join OPEN |
| TH5 clean complete mismatch | complete clean input observation, matching source attempt, durable mismatch intent | Kestrel | TH0; fixture auth only | production Agent client; test transport changes exactly one body byte after Agent signing, before real TLS, to establish corruption premise | original body once; no repeat after terminal | exact 422/O19 after durable positive-absence reconciliation, key revocation and TI02; no body replay | P19, BP20 terminal control | G-U clean EOF/cause; G-T exact positive-absence fence; G-H shape | corrected HTTPS branch and two independent negative controls PASS; both narrow guard mutations RED at their intended assertions; restored focused join 6/6 after rebuild. TH5 is component evidence, not a closed P19/BP20 row or whole-cluster gate |
| TH6 recapture, server and local | exact attempt safely settled with O20; separate expired/lost local lease | Kestrel for server half; no HTTP call for local half | TH0 for server; fixture auth only | production Agent client and owner | server retry no body; local branch zero sends | 409/O20 server; local `RECAPTURE_REQUIRED`, no DB change | P20, BP20 expiry/terminal | G-E original H; G-T exception/finalization; G-H local no-call | real ciphertext tamper → SQL settlement → HTTPS 409/O20 → Agent bodyless replay and separate Agent expired-lease 2/2 components PASS; one same-source join and current-byte guard mutation OPEN |
| TH7 receipt-loss cleanup | R5 Available with durable R6 pending obsolete key/object; original HTTP receipt dropped | Kestrel | TH0; fixture auth only | production Agent first send and a fresh Agent client for replay | no new raw body on replay | persisted Available, independent-scope R6 completes, then O17 | BP12, P22, P17 | G-L exact cleanup; G-P persisted publication; G-H replay | same-source HTTPS receipt-loss→R6 Pending→Completed→O17 component PASS; separate real process-kill/receipt-loss join remains OPEN |
| TH8 settled same-owner reentry | first request lost after durable R1, old lease elapsed but frozen H live | Kestrel | TH0; fixture auth only | production Agent clients with original metadata and lease | first request stops before R2; successor body read once after safe settlement | successor reaches O22/Available | BP20 transient, BP20 race, BP21 lost R1 | G-E exact actor/binding/head CAS/horizon; G-N witness | same-source HTTPS/Agent R1 loss → NPS01/RE01 → successor R2–R5/O22 component PASS; Process.Kill and current-byte guard mutation OPEN |
| TH9 internal encryption failure | actual R2 encryption/provider failure and safe settlement | Kestrel | TH0; fixture auth only | production Agent client | bounded first read only; no raw diagnostic leak | O18 or O20 after exact cleanup; never O23 wire | P23, P18/P20 controls | G-U writer failure/cause; G-T settlement; G-H no-egress | actual R2 AEAD unwrap failure → 503/O18 and no O23 leak; production continuation proves exact positive object absence without terminal fabrication; same-lease O16 reevaluation PASS, guard mutation OPEN |
| TH10 stage/result projection | each real R2/R3/R4/R5/R6 result including malformed/unknown controls | Kestrel where public result is claimed; SQL otherwise | TH0 for public rows | production Agent client for public rows | per state: none on bodyless continuation | exact O18/O21/O22 or fail-closed; no fabricated O20 | BP19, P18, P21, P22 | G-M exhaustive enum/shape projection | exact eight-enum/40-member declaration census component PASS; real-result producer/mapper and unknown-numeric semantics OPEN |

The `OPEN` entries above are **not** PASS and not row-level tickets. Exact shared dependencies before any closure are:

| Scenario set | Concrete unmet prerequisite/blocker at this Phase-A freeze | Allowed next control in the common arrangement |
| --- | --- | --- |
| TH0 | shared HTTPS health and production Agent transport smoke passed earlier, but use the development certificate and fixture authentication only; no production TLS/auth claim | retain the same handler for later business scenarios; no new transport harness per row |
| TH1/TH2 | paused-R5 Kestrel publication and durable O17 Agent replay pass. A separate branch on the same shared host now uses the production Agent for both first send and replay, sees `Committed` with no response/Available while the R5 transaction waits, then exact O22 and bodyless O17 after release. Fixture auth does not prove production authentication; no final-current-byte G-P/G-H mutation or whole-cluster gate has run | retain both raw-HTTP and Agent controls; run eligible guards only in the horizontal mutation phase |
| TH3 | a production Agent first send is canceled after R4 Committed while R5 waits; same-source new Agent gets genuine 202/O21 with zero additional body reads, worker publishes and later replay gets O17 (after any typed O02 evaluation gate); key/object counts stay fixed. This is an actual HTTP cancellation, **not** a `Process.Kill` on this source, and no public 200/O22 response is observed after the interruption | retain the ten-cut child matrix as a separate frozen component; same-source process-kill/O22 join remains OPEN and must not be inferred from cancellation |
| TH4 | the independent cleanup-incomplete branch is now reached through the TH9 actual AEAD failure: public O18, exact object positive-absence reconciliation, then same-lease broker reevaluation to O16 (after typed O02 evaluation delay if needed), with no extra body/key/object or fabricated terminal. Provider-unknown-to-O18/O20 remains an explicit semantic blocker, and terminal/expiry completion is still unjoined | preserve the semantic blocker, retain this as a component, and do not infer full P18/BP20 from it |
| TH5 | Original correction exposed a real B2 conflict: exact `NoObjectEstablished` with `PositiveAbsence` remained `CleanupPending` because `Terminated` required `Deleted|Quarantined`. Homeowner explicitly approved **conditional positive absence**, recorded in `a3_o19_positive_absence_authority_amendment.md`. Product SQL and continuation now require the same O19 intent, attempt/source/key/object identity, revision/fence/fingerprint, armed put operation, observed positive absence and terminal key. Corrected HTTPS/Agent focused run is 3/3 with durable final O19; `NotArmed` and positive-absence/wrong-cause controls are each 1/1. Both corresponding narrow SQL mutations are RED at `StateConflict` vs `Terminated`; SQL was restored byte-exact and a newly built 6/6 focused join is GREEN. The Down guard's complete installed-function hash audit is included. This does not close the complete P19/BP20 row. | Retain as component proof; check only in the final cluster joined gate after all independent scenarios are dispositioned. Do not broaden `TerminatedBeforeStart` or count unknown provider as absence. |
| TH6 | the shared HTTPS host drives an actual R2 ciphertext tamper before verifier; production recovery writes the O20 terminal cause, deletes object, revokes key and emits 409/O20 through the production Agent client. Same-metadata replay returns O20 without another server body read/key/object. The separate Agent expired-lease/zero-send theory has now passed 2/2 on current Agent source, but it does not use the server's exact source/lease; no new guard RED exists for this public branch | include both in the horizontal gate, but keep same-source local/server join and whole P20 row OPEN; do not substitute a synthetic `Final` |
| TH7 | The former same-source scheduling gap is closed as a bounded component: a database-local, test-only advisory lock at production R5 function entry lets R4 commit before obsolete custody is seeded, without holding a row lock or replacing R5's business decision. The first production Agent request reaches Available with two Pending R6 items; its HTTP response is dropped. A fresh production worker with owner scopes completes both obsolete key/object items, and a fresh Agent client gets O17 with zero additional server body reads and no new key/object. The exact image digests were recovered from Quay; the earlier 0/4 and 503 diagnostics remain excluded. | Do not infer that the separate four real process-kill cuts occurred on this same receipt-loss source. The process-kill/receipt-loss join, if required for row-level BP12/P22/P17 closure, remains OPEN. The focused 4/4 and two MinIO sentinels are not a whole-cluster restored gate. |
| TH8 | the direct broker RE01 successor runs R2–R5/Available while old attempt JSON and original H remain invariant. A separate same-source HTTPS/Agent control now cancels the first request after durable R1 but before R2; after the PostgreSQL-clock lease expires, the same metadata/lease reaches NPS01→RE01→R2–R5/O22 with two attempts and one key/object. This is request cancellation, not Process.Kill, and no current-byte G-E mutation has run | retain both as Phase-A components; same-source child-process kill and mutation-sensitive row closure remain OPEN |
| TH9 | the actual AEAD unwrap-failure producer traverses R2 writer, HTTPS/Kestrel, production Agent and public 503/O18 without leaking O23; independent continuation then resolves its PutOutcomeUnknown object to `PositiveAbsence / NoObjectEstablished` while attempt JSON stays invariant. Same-lease reevaluation to O16 is also observed after the typed O02 delay. An isolated guard RED remains absent | keep the producer and retry in the shared transport batch; mutate the reached projection guard only after candidate freeze, with no injected internal Final |
| TH10 | the companion's eight disposition enums now have an exact 40-member drift control, but this declaration census does not run every real producer or projection; an unknown verifier enum can still be projected as O20, and generic repository `Enum.TryParse` accepts undefined numeric text | retain the semantic/product blocker; continue a real-producer/result-shape matrix, without treating the static census as BP19 closure or inventing a `Final` |

The TLS paragraph that follows is historical diagnosis; its intermediate "not yet rebuilt" statements are superseded by the current TH0/TH1/TH2 controls and the 6/6 O19 restored checkpoint above.

The TLS failure is a **shared harness blocker**, not a P17 result. Three diagnostic TRX files (`a3-r2r6-p17-agent-kestrel-diagnostic-v2/v3/v4`, plus `v5/v6`) failed before first R5 publication at TLS handshake; they are excluded from product evidence. A transient Agent compile error was corrected before run. HTTPS edits were fully removed and the earlier HTTP Kestrel test restored to SHA-256 `F436E06801176E9F782912A03FFEB04C93C8B5D7E8FA8CAF6D8571AAE87CC0F9`. No business row is closed from those diagnostics. One BP20 expiry assertion extension was made immediately before this correction; its focused test was interrupted, so it remains an **unverified Phase-A edit**, not evidence.

The shared **minimal** HTTPS/Kestrel positive control (no PostgreSQL, MinIO, broker, Agent, or business code) reproduced `HttpRequestException → IOException: unexpected EOF` with the ephemeral in-test certificate. `a3-r2r6-shared-tls-positive-control.trx` SHA-256 `59E8C37A5966754F4F220FA5B1C7BF3B04BECEFAED0BB8160FB8AA751997ECBA` and logging diagnostic `a3-r2r6-shared-tls-diagnostic-v2.trx` SHA-256 `6D225B82A00B5813F7E81FCCC6A1F95B85C7C97B8A077FBB36FB3ACCBB68F551` were each 0/1 before the health route. Both are **harness diagnostics, not product RED**. `dotnet dev-certs https --check` found a valid local development certificate (read-only check); configuring this single shared Kestrel harness to use it resolved the transport prerequisite. The earlier health test source SHA-256 was `5E2E609C0C57624682C4DC923A77AACA9C2AAED0F7C4642136DEFB892A9F566C`; `a3-r2r6-shared-tls-final-positive.trx` SHA-256 `E908D45FFA78D9402A83C6DD3C2FE5D43334FE1FF88B8054D29E307D959D0CE2` is 1/1. Phase A subsequently moved the same shared harness into the default-compiled HTTP scenario source so that the opt-in transport smoke and opt-in Kestrel business scenario reuse it. The ordinary suite retains the historical HTTP test; only `EnableCaptureAgentA3Acceptance=true` selects the TLS business variant. Neither changed source has been rebuilt or rerun yet. The harness has no production-certificate or authentication claim. The interrupted BP20 focused run produced no accepted TRX.

## Remaining unique production-guard inventory (before any new mutation)

| Guard | Rows/scenarios it carries | Required positive/negative control | Mutation target | State |
| --- | --- | --- | --- | --- |
| G-U input observation/provider ambiguity | BP17 unknown, P18/P19/P21/P23; TH3–TH5/TH9 | distinct clean EOF, provider unknown, failed reader, actual encryption error | cause precedence, never infer no-start/mismatch from unknown | provider-unknown semantics BLOCKED; other controls OPEN |
| G-T TI01/TI02 exact cause and settlement | BP20 terminal/expiry, P18–P20; TH4–TH6 | intent commit, key/object safe settlement, final code, old operational bytes | reject already-terminated O20 exception or bypass intent fence | BP18 sentinel frozen; expiry extension unverified |
| G-E RE01 actor/binding/head/original-H | four BP20 rows, BP21 lost R1; TH6/TH8 | same owner + terminal + different owner + stale CAS + post-H | owner check, head CAS, horizon | prior RE01 components PASS; joined OPEN |
| G-S R3 stage CAS/intent exclusion | BP20 race, P21/P22; TH3/TH8/TH10 | exact observed tuple, two lock orders | stale tuple or absent intent check | BP18 sentinel frozen; remaining joined OPEN |
| G-C R4 commit from fresh tuple | P21/P22; TH3/TH10 | staged→committed, no premature Available | stale CAS or skipped commit | prior component exists; joined OPEN |
| G-P R5 durable publication gate | BP11, BP12, P17/P21/P22; TH1–TH3/TH7 | committed not visible; published visible after commit | `Committed→Available` | BP11 candidate component complete; transport join OPEN |
| G-L R6 exact cleanup/receipt-loss | BP12, P18/P22; TH4/TH7 | exact obsolete object/key, independent worker scope, lost receipt | skip cleanup or wrong winner selection | same-source HTTPS-loss/R6 component PASS with skip-cleanup RED; separate process-kill join OPEN |
| G-M stage-result enum projection | BP19, P18/P21/P22; TH10 | all real enum members and impossible shapes | unknown enum must not fabricate O20 | semantic blocker for unknown enum; others OPEN |
| G-H public result/body/Agent | BP11/BP12/P17–P23; TH1–TH10 | real route, wire bytes, Agent parser, zero body-read where due | pre-body guard; wrong status/code/shape | shared TLS harness PASS; business joins OPEN |

Guard G-V durable ciphertext and G-N NPS evidence are frozen completed components; do not mutate them again. Phase A must finish all planned product/test edits across independent guards before building the shared candidate baseline. Then run horizontal phases B–J once: B build; C positive controls; D guard mutations; E exact-byte restore; F one rebuild; G joined restored gate; H ratified sentinels; I manifest/drift/mutant sweep; J one cluster packet. Any newly discovered product defect resets the candidate phase; no earlier gate may be called final.

### Guard owners and Phase-A edit ledger

This is the mutation inventory for the whole remaining cluster, **not** a sequence of row tickets. A guard with no reached positive control is not mutated merely to manufacture a RED. Before Phase D, each eligible line must have a frozen positive-control identity and an isolated proposed edit; if that cannot be done, its mapped scenarios remain OPEN with that precise cause in the final cluster packet. Existing mutation records for G-P/G-H are historical controls, not permission to skip the restored joined gate.

| Guard | Exact owner/decision to target | Phase-A control arrangement | Planned isolated mutation or blocker |
| --- | --- | --- | --- |
| G-U | `CaptureRuntimeRawIngressBodyPipeline.ProjectObservation` and `RawExportR2FramedCiphertextStream` complete-vs-interrupted observation | one input matrix with clean complete, short EOF, transport interruption and unknown provider; same durable attempt lineage | interchange `TransportInterrupted`/`ContentCommitmentMismatch` only after a real producer reaches both; provider-unknown O18-vs-O20 semantics remain a named blocker |
| G-T | `RawExportR2TerminalIntentRecorder` + migration `raw_export_finalize_retained_r2_terminal` and exact O19 `Terminated` positive-absence exception | TI01 no-key, TI02 live-key/post-expiry, exact attempt and non-attempt row text; TH5 O19 plus separate `NotArmed` and wrong-cause negatives | relax one finalization settlement/cause fence; for O19 exception include `NotArmed` in the terminator's accepted object state or remove the exact cause guard, expecting the corresponding negative to fail; preserve independent write guard and report its effect rather than claim a bad commit |
| G-E | migration `raw_export_reenter_retained_source` state/owner/head/original-horizon gates | one old-attempt matrix with terminal/different-owner/stale-head/live-H/expired-H and a real successor R2–R5 continuation | remove one owner or horizon predicate only after the corresponding positive/negative pair is green |
| G-S | `RawExportR3Repository.StageAsync` → stage SQL exact tuple and terminal-intent fence | concurrent TI/R3 two-order ratified sentinel plus successor CAS control | frozen BP18 intent mutant not repeated; stale-tuple mutant only if new CAS join adds independent observation |
| G-C | `RawExportSourceFinalizationRepository.CommitAsync` → commit SQL | staged/no-publication and stale-CAS controls in the same continuation arrangement | stale observed-tuple bypass; do not re-encrypt or fabricate a stage result |
| G-P | `CaptureRuntimeSourcePipeline` publication projection and R5 publish | TH1/TH2/TH7 share one Kestrel host and independent observer | historical `Committed→Available` mutant; rerun only when relevant product bytes change or in required guard phase with exact current baseline |
| G-L | `CaptureRuntimeSourcePipeline.AdvanceCleanupAsync` and existing R6 cleanup SQL | TH7 lost receipt plus frozen 4 cleanup kill cuts | isolated skip-cleanup mutant RED at pending→completed assertion; wrong-winner mutant not run, so no claim for that guard |
| G-M | real stage result DTO/enums and `CaptureRuntimeSourcePipeline` projection | SQL-stage enum census plus each reachable producer and unknown-cast negative | unknown enum currently can fabricate O20 per prior review: semantic/product blocker, no synthetic Final substitution |
| G-H | `CaptureRuntimeRawIngressAdmissionService` pre-body final and `RawExportSourceIngressEndpoints.MapRuntimeResult` | shared TH1–TH10 host, exact response field sets and server `Request.Body` counter; production Agent for claimed client rows | historical final-before-body forced-read mutant; new status/body/shape mutant only against a reached real result |

Phase-A source edits currently comprise: one shared `R2R6LoopbackHttpsHarness` reused by the opt-in health/Agent transport smokes and the opt-in Kestrel business variant (ordinary default HTTP test preserved); the HTTPS business variant feeds actual durable O17 replay to production `CaptureRuntimeHttpClient`, while its first R2–R5 send still uses raw `HttpClient`; the same arrangement has an O19 branch with one-byte in-transit corruption after Agent metadata validation and a receipt-loss branch where the first production Agent send reaches R5 but its already-produced HTTP response is discarded before a fresh Agent client replays O17. RE01/TI01/TI02 reentry/residue extensions also remain Phase-A edits. The synthetic O17 returned by the Agent transport smoke is a **harness prerequisite only**. The first O19 continuation was blocked by B2's `Deleted|Quarantined` rule. Following explicit Homeowner authority for conditional positive absence, product continuation and exact SQL fences were corrected; `a3-r2r6-positive-absence-correction-v2.trx` is a focused 3/3 on the corrected HTTPS/Agent candidate with final O19. A first `NotArmed` negative diagnostic failed at a test assumption about its observation timestamp and was excluded; the corrected `NotArmed` negative and independent positive-absence/wrong-cause negative are each 1/1. `a3-r2r6-down-guard-audit.trx` is 1/1 across all installed A3 function bodies pinned by the Down guard. The positive-absence exception still requires mutation, restored joined gate, sentinel sweep and final packet. Earlier 6/7 and 3/3 diagnostics remain historical, not a restored gate. A filter typo produced a zero-test TRX and is excluded. Product correction **reset the candidate phase**; remaining independent Phase-A scenario edits precede a new shared baseline and any eligible Phase-D guard mutation.

O19 correction checkpoint (not a cluster final gate): migration baseline SHA-256 `4FE0E4C7FECCE210C8DD34BA9066E97BF776F69FA3E752B529F779E2550EA7E4`. An isolated terminator mutation admitting `NotArmed` produced mutant SHA-256 `2C27C6F519343719C512084FD5AAF68DA941BBB46C24E94714680D542B1FEC47`; `a3-r2r6-o19-notarmed-guard-red.trx` SHA-256 `A49881CEF1A3315F07613753317AD940E9226CF7064869D33B53A1D6B533D1E8` was 0/1 at the target (`Expected StateConflict`, `Actual Terminated`). After byte-exact restoration, an isolated cause mutation accepting any non-null terminal intent produced mutant SHA-256 `77EB5D570D96CA3C19DCC7F2DFB5259832921295F3278273C2A8F29CEDC73A60`; `a3-r2r6-o19-wrong-cause-guard-red.trx` SHA-256 `EC40D37DBD218ADB1DEDA46F8FD4137642C1E98EF2F767F0C0EACDFF7F5058A8` was 0/1 at its target (`Expected StateConflict`, `Actual Terminated`). The migration was restored again to baseline and rebuilt, and `a3-r2r6-o19-restored-joined.trx` SHA-256 `8907756FB7A201A2360D313757C4FD317A1B6CA5957A6B8064BFAA1889C9A910` passed 6/6 with zero fail/skip. The separate ratified-sentinel run `a3-r2r6-o19-ratified-sentinels.trx` SHA-256 `B1B92174A37FB6B7B89DDCA74D941BD8EE29BA17A961EF58762BF59595EB5BDE` passed 10/10, including the ten-cut child-process matrix as one test, four BP21 evidence cases, two BP17 ciphertext cases, two BP18 race cases and BP16 ciphertext-source verification. This checkpoint occurred before every independent TH scenario was dispositioned, so it is **not** represented as the directive's horizontal Phase-D/G final gate; remaining scenarios and eligible guard mutations must still be handled in the cluster-wide pass.

TH7 retry diagnostic (excluded): a database-local scheduling shim was tried at R5 entry so obsolete custody could be seeded after R4 commit without holding R5 row locks. `a3-r2r6-th7-entry-gate-diagnostic.trx` SHA-256 `0B30A78D1525CA7FFD5966F2C1A97D74D85079855D31CC6198882A5D672F357C` was 0/4 **before the TH7 assertion**: the new case failed at test-only function-text insertion, while the three existing cases could not start MinIO because the pinned `minio/minio@sha256:14cea493d9a34af32f524e538b8346cf79f3321eff8e708c1e2960462bd8936e` image was absent. A direct pull of that exact digest returned `pull access denied`. The scheduling attempt and helper edits were removed; the two touched test sources were restored byte-exact to SHA-256 `60B3A3C146FDE68808D7A58BF147B47CC2ED883B36FB184870CDC8322FA7EC27` and `F7CEF0FCC93FA1D4D4EC4AA24CFF027A3E27CAF4FD3CEBC7E60DE8BDF957D3B7`. No TH7 or product result is inferred from 0/4; the preceding 3/3 and O19 6/6 remain the last valid focused baselines, not a new gate. At that diagnostic checkpoint TH7 stayed OPEN; the successor below supersedes those two blockers without retroactively counting the diagnostic.

TH7 recovery successor (bounded component, not a whole-cluster gate): Docker Hub pulls of the pinned MinIO digests failed, but the identical immutable digests were retrieved from `quay.io/minio/minio@sha256:14cea493d9a34af32f524e538b8346cf79f3321eff8e708c1e2960462bd8936e` and `quay.io/minio/mc@sha256:eb4ea9884b77704230e2423e9004d2fa738dc272876b9cc41a297d29443b8780`. The fixture and matching image assertion changed only registry host, not digest; MinIO sentinels `a3-r2r6-th7-quay-fixture-sentinels.trx` SHA-256 `6D32C1CACEF160CBFE491653827EB820D9A61C02ADBE786B05CF8BCC9E8B806D` passed 2/2. The earlier image-unavailable blocker is superseded; no historical diagnostic is recast as product evidence.

The corrected same-source test uses an isolated database-local scheduling shim inserting one advisory transaction lock before production R5 row-lock acquisition. It does not synthesize an R5/R6 result. `a3-r2r6-th7-quay-hardened-focused.trx` SHA-256 `C492B3FFDF404DBD75B953AA65A487699752F7E6D817F2E44A2FB2D889873902` passed 4/4, including the new `(contentMismatch:false, dropResponse:true, withObsolete:true)` case. An isolated product mutant skipped only `Available + Pending` cleanup dispatch in `CaptureRuntimeSourcePipeline.AdvanceAsync` (mutant SHA-256 `D4E2FBF893188198CC035634EB9AE8F16AE5476F64647BB07CC8870BD285074D`); `a3-r2r6-th7-skip-cleanup-red.trx` SHA-256 `44DC0101C3EA6A647E6D4B04F1B44F559A5D93B8E99ED326FC730C1E969FE175` failed 0/1 at `Assert.True(completed)` in the new case, not setup. Product source was restored byte-exact to SHA-256 `399F7D61E2E5C4789D58842F4F7E7CF84E789FE5E84399CCF951D02F881036F2`, rebuilt, and `a3-r2r6-th7-quay-restored-four-scenario.trx` SHA-256 `149F985C270F6A97A4C70F95836F21FA8B2DEB97165C984B6E85400B3272CD32` passed 4/4 with zero skip.

The new case observes R4 Committed, R5 Available with `CleanupDisposition=Pending` and exactly two cleanup items, then runs a fresh production worker in separate owner scopes. It observes `Completed`, old key Revoked, obsolete SQL object Deleted and MinIO `PositivelyAbsent`, while winner key/object and attempt history remain unchanged; fresh Agent replay is O17 with no new body read. This is same-process worker recovery after receipt loss. It does **not** claim a `Process.Kill` on this same source, whole-cluster Phase G/H completion, P-row closure, or A3 PASS.

O19 Phase-A negative-family control (not a row closure): `Ti02_O19PositiveAbsenceNegativeFamilyKeepsUnprovenAndMismatchedAttemptsPending` uses one MinIO fixture and an isolated disposable PostgreSQL database for each of seven table entries: provider-unknown, observed-unknown without positive absence/quiescence, actual object present, wrong principal binding, wrong revision, wrong fence, and an exact positive-absence control. All cases call production `raw_export_terminate_source_encryption_attempt`; negative cases require `StateConflict` with exact attempt and resource rows unchanged, while the positive control requires `Terminated` then TI02 `Finalized` with O19. The test-source SHA-256 is `C4A148D98E63E2E13139DD37BF1F62FAA575567BD20A725882C5D3E62FD2A518`; focused `a3-r2r6-o19-negative-family-baseline-v3.trx` SHA-256 `102C17482BC8119D941FF0D369F3C73FA7A885A2FAEC8108551A5DFAF3B0A990` passed 1/1 (seven branches within one method). Two earlier focused runs are excluded setup-assertion corrections: unknown-provider observation legitimately has a timestamp, and its durable `PutOutcomeKind` is `OutcomeUnknown`. No product mutation, restored cluster gate or P19 closure is inferred from this Phase-A positive control. After remaining scenario edits are frozen, G-T's eligible negative-family guard mutations belong in the shared horizontal mutation phase.

TH9 Phase-A producer control (not a row closure): the shared HTTPS/Kestrel theory now has a fifth branch where a test-only `IKekOperationProvider` wrapper delegates real key preparation but throws on the bounded AEAD unwrap. The production R2 writer, object PUT, admission, endpoint mapper, real socket/TLS and production `CaptureRuntimeHttpClient` execute. A transparent response wrapper observes exact HTTP 503 and the single-field `outcomeCode=RAW_EXPORT_SOURCE_TEMPORARILY_UNAVAILABLE`; `SOURCE_ENCRYPTION_FAILED` does not appear on the wire. The Agent returns O18, no publication or terminal intent/outcome is fabricated, and the durable object remains `PutOutcomeUnknown`. Test source SHA-256 `5D830A4D98A26D46450185EFE0DF8C284E79723314646FCECE302512591FEA61`; rebuilt focused `a3-r2r6-th9-encryption-failure-wire-phase-a-v2.trx` SHA-256 `48E0615873918153E779651A4E9952F4DCBF5B6D6A2D834C7E7B9F766FEB218E` passed 1/1. An earlier 0/1 focused run SHA-256 `48DA3D6523010243D26189205D89120E0E6B608B7F8467E6C282B59053FA274E` failed at PostgreSQL bootstrap (`NpgsqlException`/socket abort), before the test body, and is excluded. A later source edit added the wire assertions, so the earlier 1/1 producer-only run is historical. This does not prove safe settlement, retry, a reached guard mutation, or P23 closure; TH9 remains component-only until horizontal candidate phases complete.

TH9 Phase-A recovery successor (still not a row closure): the same shared scenario calls `CaptureRuntimeSourcePipeline.AdvanceAsync` after the actual AEAD failure. Its exact S3 reconciliation commits `PositiveAbsence / NoObjectEstablished` from the durable `PutOutcomeUnknown` state; no terminal intent/outcome or publication is created, and the entire attempt `to_jsonb` text is unchanged. Test-source SHA-256 at this TH9 checkpoint `44F8F9A8CEA828366BC20E1488AB87C6708CBBFA82BE92594654CEB7C9292BC0`; rebuilt focused `a3-r2r6-th9-encryption-reconciliation-phase-a.trx` SHA-256 `C776E04AA40DD3AD6775E396DFF34DE310B935640C49D39A26E06C12A2D9FB35` passed 1/1. The earlier wire-only 1/1 is historical because its test source predates this added recovery assertion; this 1/1 is also historical after the later TH3 edit, pending the horizontal joined gate. This positive control does not prove a same-lease retry or a mutation-sensitive guard; those belong in the shared horizontal phases, not a separate P23 packet.

TH10 Phase-A catalogue control (not BP19 closure): `R2R6StageDispositionCatalogueMatchesCompanionContract` is an independent no-PostgreSQL method checking the exact 8/3/5/5/5/4/5/5 declaration partition (40 members total) directly from the eight production enums, with distinct numeric values **within each enum** and no copied enum-derived expected count. Test source SHA-256 `375D037ED9BBE6DF3EF600DB712A49592F2185EB6312EC61F009C701F3A08F5D`; rebuilt focused `a3-r2r6-th10-stage-catalogue-phase-a.trx` SHA-256 `6E8867D753660A64D588A603509B5FA3062F1EF5537A29AB2A08F93A6F606627` passed 1/1 in 12 ms. It only prevents enum declaration drift. It does not exercise the real producers or mapper, invalid-shape controls, unknown numeric parsing, HTTP or Agent; those remain OPEN and no row/census flip follows.

TH8 Phase-A successor component (not a row closure): `Re01_SettledSameOwnerSuccessorRunsExistingR2R5WithoutOldAttemptRewrite` waits for the original lease by PostgreSQL clock, obtains a distinct same-owner attempt from the production broker/NPS01→RE01 path, and passes that exact handoff and original admission context to the production `CaptureRuntimeRawIngressBodyPipeline`. The existing R2 writer and R3/R4/R5 continuation reach one Available publication for the successor. The original attempt's full `to_jsonb` text and original horizon remain unchanged; because the predecessor is deliberately pre-provider, no old key-reservation row exists before or after. Test source SHA-256 `AC6721EC2C6DAF8C18E759E3AFE6C4954C3B3128C5FA23EC1B6CEDAAA457CFD8`; rebuilt focused `a3-r2r6-th8-successor-r2r5-phase-a-v2.trx` SHA-256 `3163A770FDFD3F4D3801FFFCBF9160868BEF93E3F3CC11DDFC9DB46FAB2AF2F6` passed 1/1. The first 0/1 diagnostic SHA-256 `CEA397C9948635CBB9673201F4A3B9CF0F0C10F403923C5194156D791CFC6E1A` failed only because the test incorrectly required a pre-provider key row; it is excluded, not a product RED. This control does not traverse the public HTTPS route or Agent same-lease retry and has no isolated mutation; BP20/BP21/P22 remain open.

TH3 Phase-A same-source cancellation component (not a process-kill or row closure): the sixth branch of the shared HTTPS/Kestrel theory starts a production Agent first send, waits until the independent observer sees R4 `Committed` while the test-only R5 advisory gate holds, then cancels that **real** request before a response. R5 has no Available commit. A fresh production Agent client with the same metadata and lease gets genuine 202/O21 with zero additional server body reads or key/object rows. A production continuation then publishes to Available. A later Agent replay, after the broker's typed O02 evaluation gate (waiting for its returned `RetryNotBeforeUtc` against PostgreSQL clock when present), gets O17 with the original source ID and no new body/key/object work. No Final is injected. Test source SHA-256 `3A3693B1C836365A236BC5BCA6DC42D0765A07ED224754D0E24B2B60A57973C0`; rebuilt focused `a3-r2r6-th3-cancel-at-committed-phase-a-v3.trx` SHA-256 `B25F1B960A11247D9F408E54D117995BC43B3021316A9EE9020BCC7DCF571FD9` passed 1/1. The first 0/1 diagnostic SHA-256 `A36088D24FB43E1F60C601B6227079934F6BAFCB0047C3D88AE89A9D1FF23ABE` had already passed the O21/worker controls but failed because it incorrectly expected O17 before O02's retry time; it is excluded. This does **not** join a real child `Process.Kill` to the same source, does not observe 200/O22 after the interrupted request, and has no new guard mutation. P21/P22 remain OPEN pending the horizontal candidate gate.

TH6 Phase-A public terminal component (not a whole P20 row): a test-only database trigger holds the production R2 `ObjectPresentPendingVerification` record transaction only after the real MinIO PUT has completed. A test-root MinIO write then corrupts the exact object before the verifier reads it. Release lets the production verifier, terminal-intent recorder, object cleanup, key revocation, TI02 finalization, endpoint mapper and HTTPS transport complete. The production Agent client receives 409/O20 with only `outcomeCode`; observer confirms no publication, `R2TerminalOutcomeCode=RECAPTURE_REQUIRED`, object `Deleted` and key `Revoked`. A second Agent client with the same metadata receives O20 with no new `Request.Body` reads or key/object rows. The scheduling trigger and corrupting root write are premise controls, not replacement business decisions or a synthesized Final. Test-source SHA-256 `C814232B95ACEF8A60F1E400E0635DCFCF85B7139E7DFBB2E56D349FBF434BB6`; rebuilt focused `a3-r2r6-th6-o20-replay-http-phase-a.trx` SHA-256 `7592508B35331F09E1141CD5434BCCAD43CB1125AEE34798B59EEE9FB44AAD96` passed 1/1. The earlier one-send O20 run is historical after the bodyless replay assertions were added. The local Agent lost-lease/zero-send control and public branch are not yet one joined gate, and no current-byte isolated guard mutation is claimed; P20/BP20 terminal remain OPEN.

TH4/TH9 Phase-A reevaluation successor (not P18/P23 closure): the actual KEK-unwrap-failure branch now continues after its O18 and durable `PositiveAbsence / NoObjectEstablished` object resolution. A fresh production Agent client retries the **same** metadata and lease. The broker may first return typed O02 while the evaluation token is live; the test waits for its returned `RetryNotBeforeUtc` using PostgreSQL clock, then requires O16 `ReservationBusy`, not O20 or an Available result. Server `Request.Body` reads, key/object row counts and the absence of terminal outcome remain unchanged. Test-source SHA-256 `365109932F67502430AA312FC1EE959D0A9115CEC0049B44468C11657FCFCF74`; rebuilt focused `a3-r2r6-th4-th9-retry-phase-a.trx` SHA-256 `F3BF7D6DED08B54456710EBA23D0A47FD2F627806C99663B7478BB9386E58D51` passed 1/1. The preceding TH9 1/1 runs are historical after these new assertions. This demonstrates one independent cleanup-incomplete reevaluation, not the unresolved provider-unknown semantics, lease-expiry termination or a mutation-sensitive guard.

The first shared Phase-A transport baseline after these edits, `a3-r2r6-seven-scenario-phase-a-joined.trx` SHA-256 `278A7D598318FA93C12E51A0A7E4CE512C6DFCACF8A068C0A4A9889067E7B469`, passed **7/7**, zero fail/skip on test-source SHA-256 `365109932F67502430AA312FC1EE959D0A9115CEC0049B44468C11657FCFCF74`. It contains the four preserved HTTPS branches (publication/O17, O19, receipt-loss, receipt-loss/R6) plus TH9/TH4 actual encryption failure and reevaluation, TH3 canceled-request/O21, and TH6 ciphertext-tamper/O20. This is a shared current-source **baseline**, not the directive's final restored gate: TH8's direct RE01 successor and TH10's declaration census are separate methods, no horizontal current-byte mutations have been run, and the same-source process-kill/Agent and provider-unknown/unknown-enum blockers remain.

TH8 HTTPS/Agent Phase-A successor (component only): the eighth branch in the shared Kestrel theory starts a production Agent request and a transparent test-only body-pipeline scheduling gate records its durable R1 handoff before any R2 key/object or body read. The caller cancels that real request. After the original ownership lease expires by PostgreSQL clock, a fresh production Agent client sends the same metadata and lease; production NPS01/RE01 creates a distinct attempt and the existing R2–R5 pipeline returns O22/Available. It asserts the same source ID, two attempts, one key/object, no old-attempt key, predecessor `TerminatedBeforeStart` and unchanged original H. The predecessor attempt JSON changes after NPS termination, so this test does **not** claim byte-equality for that row or isolate every changed column. Final test source SHA-256 `72FAE8F9FDACB62E5720E3008C96F5181C315CE8CB0C2D3C470DF9DFF0ABCE06`; rebuilt focused `a3-r2r6-th8-agent-reentry-phase-a.trx` SHA-256 `FD18901D97C98AD07C5025E370F6343039773A2855159D3DE18BDCB74C2DE8B5` passed 1/1, zero fail/skip. A preceding compile failure was test-block placement before host/client variables existed and is excluded; no business path ran in that diagnostic. This is actual HTTPS request cancellation, **not** Process.Kill, and no isolated guard mutant or final joined gate follows from this focused control. The earlier seven-scenario TRX is historical after this source edit; candidate Phase A now has eight branches.

The eight-branch shared HTTPS/Kestrel Phase-A gate `a3-r2r6-eight-scenario-phase-a-joined.trx` SHA-256 `F30FC6C2F009D35B76A9EB6CC15F16554A5C6B129A7EE51D25DD4E8E468D761D` passed **8/8**, zero fail/skip, on the same test-source SHA-256 `72FAE8F9FDACB62E5720E3008C96F5181C315CE8CB0C2D3C470DF9DFF0ABCE06`. It contains all seven prior transport branches plus TH8 same-source R1-loss/reentry; it took 1m44s because TH8 waits for the real ownership lease. This is a candidate positive-control gate, **not** a mutation-restored whole-cluster gate or full suite. No R2_R6 row/census state changes follow from it.

TH1 Agent first-send successor (component only): a ninth branch uses the same shared HTTPS/Kestrel setup but replaces the ordinary raw `HttpClient` first send with production `CaptureRuntimeHttpClient`. While a test-only R5 advisory lock holds, the independent PostgreSQL observer sees a Committed publication but no Available head/publication and the Agent send is still pending. After release the Agent receives O22, with one Available publication and the exact source identity. A fresh production Agent client then gets O17 with zero additional server `Request.Body` reads and unchanged key/object counts. Test source SHA-256 `0588D3FCCDFA8E77B296FCF6C7B8A013E724F4D03D975DC3DA8B947F9E1EC4A3`; rebuilt focused `a3-r2r6-th1-agent-r5-phase-a.trx` SHA-256 `2642950383728D39294B06757F3984771C415C020BA00BD3EA03EE13D5C47E70` passed 1/1, zero fail/skip. A preceding compile failure from a conflicting local test variable name is excluded; no business path ran. The eight-scenario TRX is historical after this test-source edit. This does not prove production authentication or complete P22/BP11 ratification, and no fresh mutation-restored whole-cluster gate has run.

G-E/RE01 shared mechanism control: on the current product bytes, one focused run of all six `Re01_` methods passed **6/6** with zero fail/skip (`a3-r2r6-re01-mechanism-phase-a-joined.trx` SHA-256 `E33BC4BF9F67F8C8057EBF89B89C15BB0E86D8E5397A791DE6CF7B0B4FD64CFC`). Its identities are `LostR1RetryAppendsOneFreshAttemptAndRenewsOnlyDerivedLease`, `ConcurrentSameOwnerRetryHasOneHandoff`, `UnknownProviderCannotReenterOrEraseEvidence`, `InternalHelperRejectsWrongActorBindingTupleAndStageRoles`, `TerminatedNoObjectStillRequiresSettledKeyAndPreservesOldRows`, and `SettledSameOwnerSuccessorRunsExistingR2R5WithoutOldAttemptRewrite`. This reused pre-existing/current tests rather than adding a row-specific fixture and took 3m47s because several cases wait for real database leases. It is a positive/negative **component join**, not a fresh current-byte G-E mutation, HTTPS Process.Kill join, complete BP20/BP21 closure or final cluster gate.

TH6 local Agent component: `A3_ExpiredLease_RetiresAndRequiresRecapture` ran both existing theory cases on Agent test source SHA-256 `CBEE084567A367F2A85624CC5F0D5907558C29ED646943377AF078DA05AA9CF6`; `a3-r2r6-th6-agent-expired-lease-phase-a.trx` SHA-256 `24F623E9A378D5A7C6577AB0752BC85B5A69A77833BB0EE8DFF94426FC8B9083` passed **2/2**, zero fail/skip. These are local no-send/recapture controls, not the same lease used by the public O20 source; they will be a separate Agent-side sentinel in the horizontal gate and cannot alone close P20.

The frozen nine-branch HTTPS/Kestrel candidate baseline `a3-r2r6-nine-scenario-candidate-baseline.trx` SHA-256 `6C51FA77E62B370199E11A1B203BF181C6050B39F15A6E44A799D54597FA95C5D` passed **9/9**, zero fail/skip, on test source SHA-256 `0588D3FCCDFA8E77B296FCF6C7B8A013E724F4D03D975DC3DA8B947F9E1EC4A3`. This is a positive-control freeze before the following horizontal current-byte mutations, not the restored gate.

Horizontal G-P/G-H mutations on that frozen test source:

| Guard | Mutant source SHA-256 | Targeted TRX SHA-256/counters | Exact result and disposition |
| --- | --- | --- | --- |
| G-P diagnostic, relax only `PublicationState=Available` to `Available or Committed` while leaving `CustodyState=Available` | `A3C1F5F19D792058D6D17D91867ECC0E9AD6947B95394E328B4401F30DB94AA5` | `a3-r2r6-gp-agent-r5-red.trx` `F77BA2BFBFE5E3C9AFA75B4AF4752380A8EEFA3EBFFAA05CE833FEF625954C34`, focused **1/1 GREEN** | **Excluded as an ineffective mutation**, not counted as proof. At the R4 boundary custody is still `Staged`, so this edit never admits the forbidden tuple. Restored immediately to product SHA `399F7D61…1036F2` before the valid variant. |
| G-P valid paired-state mutation: project `Staged/Committed` as if `Available/Available` | `378B2DDC36BFD1C71598CDA07F3C669C9853002F20F67FE5F8174D1393389757` | `a3-r2r6-gp-agent-committed-red-v2.trx` `379638B98B5BED58B6588A11FEC0A8604D39772136EB5064D0F8923DE929F0FA`, **0/1 RED** | Agent first-send case fails at the R5 advisory-waiter assertion (`Expected True / Actual False`): the mutant returns O22 before ever entering R5. This is a reached production projection guard and demonstrates premature completion; it does not show a bad SQL commit. |
| G-H final-result body read: final broker result consumes one request byte before projection | `864AB93DE457C49DBE009EF94AB2A69DC3D60E6242D8F54E0568AE882C72D550` | `a3-r2r6-gh-agent-replay-read-red.trx` `9B50285D203843F317B70CA48ADAA039030E56541E54C837D790CC027389D9C2`, **0/1 RED** | The same Agent first-send case reaches O17 replay but server `Request.Body` reads rise from 2 to 3 (`Expected 2 / Actual 3`). The wire/parser still succeeds; the RED isolates the forbidden replay read. |

Both product sources were restored byte-exact before the successor gate below: pipeline `399F7D61E2E5C4789D58842F4F7E7CF84E789FE5E84399CCF951D02F881036F2`, admission `87F6741623601E1B6150F7DB025BEC5A37A6D0ADC1650063838CD3BA14D55A63`. None of the RED runs itself is a candidate PASS.

Restored horizontal gate successor: after byte-exact restoration, the IntegrationTests project was built once with `EnableCaptureAgentA3Acceptance=true` (0 errors; 58 existing warnings). `a3-r2r6-cluster-restored-joined-final-candidate.trx` SHA-256 `57ACAFEA0BB576F25761250984EC4D5CA4755C1CA93E96531F1BCAC9E6B130BA` passed **27/27**, zero fail/skip in 9m12s. The exact identity partition is shared HTTPS nine; RE01 six; O19 seven-branch negative family one; TH10 catalogue one; ten-cut actual child-process matrix one; BP16 one; BP17 ciphertext two; BP18 race two; BP21 provider-evidence four. This is a focused restored **cluster** gate, not a full integration suite. The Agent expired-lease theory was a separate current-source 2/2, not part of the 27. G-E owner/head/horizon, G-M undefined numeric and same-source HTTP+Process.Kill joins remain OPEN with explicit gaps in the scenario matrix; this gate does not automatically change any row state or the official census.

## Failed-run audit

The documentation-only successor `a3_r2r6_trx_inventory.tsv` enumerates all 109 cluster-named TRX in Server and Agent; `a3_r2r6_failed_run_census.tsv` assigns stable locators `FR001`–`FR048` to every `ResultSummary=Failed` run, including files not cited above. The 48 failures partition into 11 accepted bounded `EVIDENCE_RED`, 9 `SUPERSEDED_RED` with explicit successor TRX, and 28 `EXCLUDED_DIAGNOSTIC` with per-file reasons. `FR006 → FR005` is the BP16 consumer repin; `FR011 → FR010` is the BP21 four-evidence repin. **BP11 final accepted G-P RED is `FR013` (`a3-r2r6-gp-agent-committed-red-v2.trx`)**, independent of BP21. The old BP11 `FR001`–`FR003` are superseded for the final candidate, not silently counted as three current-byte mutations.

The shared TLS provenance is `FR022` (preliminary reachability), `FR023`–`FR026` (P17 v3–v6 TLS EOF), `FR034`/`FR033` (minimal shared-harness TLS EOF), then `a3-r2r6-shared-tls-devcert-diagnostic.trx` 1/1 and `a3-r2r6-shared-tls-final-positive.trx` 1/1 using the local development certificate, followed by the business HTTPS controls. This sequence resolved one harness prerequisite; the failed runs are not P17 product REDs. PostgreSQL bootstrap, TH7, TH9, O19, phase-c and terminal-unknown runs each have their own SHA/counters/test/error/reason in the census rather than a collective exclusion label. The corrected packet's `#failed-run-audit` section is the review handoff. No scenario or historical row status changes from this audit; official open stays 63 and mechanical candidate open stays 61.
