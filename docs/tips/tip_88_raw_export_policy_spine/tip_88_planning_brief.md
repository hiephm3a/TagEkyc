# TIP-88 (SUPERSEDED - split 2026-07-10, 88B further split 2026-07-12; 88B2 renamed -> 88B2/88B3 clean sequence 2026-07-19)

This combined "policy-gate foundation" brief was split for adversarial-verifiability. Current shape (governance index):

- **TIP-88A - Raw Export Policy Catalog Foundation** (INERT data spine; no runtime export capability): `docs/tips/tip_88a_raw_export_policy_catalog/`. **LANDED + PUSHED** (commit `e63cdf9`, closeout `62fed65`) - the 6-table append-only catalog.
- **TIP-88B - Raw Export Enforcement Spine** was itself SPLIT into two slices (too large as one): see `docs/tips/tip_88b_raw_export_enforcement_spine/tip_88b_planning_brief.md` (superseded pointer). The current pieces are:
  - **TIP-88B1 - Raw Export Control Plane**: identity surface + exact-version grant(+revoke) + fulfillment(+scoped recorder authority) + revocable runtime lifecycle + fail-closed activation gate + lock-capable effective-state resolver. Append-only, DB-enforced. **LANDED + PUSHED** (commit `2c684cf`; docs closeout `075bdcd`, pushed 2026-07-20). **NOT authorization/evidence/permit/package - those are TIP-88B3.** `docs/tips/tip_88b1_raw_export_control_plane/`.
  - The former **TIP-88B2 - Raw Export Authorization Decision** was SPLIT (review round-1) - subject consent is greenfield + a mandatory pre-authorize gate, so it is its own slice. Renamed 2026-07-19 to a clean sequential order (was awkward `88B2a + 88B2`):
    - **TIP-88B2 - Subject Export Consent**: **LANDED + PUSHED** (commit `8cd52a3`; closeout `tip_88b2_closeout.md`). Asymmetric session gates - `Granted` requires `Completed`, `Withdrawn` requires session-exists only; split per-event direct-insert invariants; pinned resolver lock contract. Greenfield append-only subject consent (scope = session + subjectRef + policy + version + SERVER-DERIVED purpose + SERVER-DERIVED recipient) + dedicated recording/withdraw authority (deployment-only grant/revoke) + latest-event-overall/no-fallback on BOTH the consent and authority aggregates + TargetRevision + pinned event row-shapes + full DB privilege/SECURITY-DEFINER manifest + server-computed ConsentScopeHash with ONE pinned single-bigint advisory-lock mapping + golden vectors + a 3-parameter resolver TIP-88B3 consumes. RECORDS/RESOLVES the consented raw-class set only; class-coverage + denial causes are TIP-88B3's. **CLOSED / LANDED at `8cd52a3`.** `docs/tips/tip_88b2_subject_export_consent/tip_88b2_planning_brief.md` + `tip_88b2_closeout.md`.
    - **TIP-88B3 - Raw Export Authorization Decision + Permit** — **NEXT planning slice. State: `UNBLOCKED_BY_TIP_88B2_LANDING`, but NOT READY_FOR_BUILD / NOT build-lockable** until its 4 post-landing gates close: gate-1 (recon landed B2 resolver/schema) CLOSED, gate-2 (B1 fulfillment-expiry projection = TIP-88B1-E1) CLOSED, **gate-3 (TIP-88A-E2 policy TTL field + bounds contract, committed `09c9359`) CLOSED**, gate-4 (REWRITE the brief from the landed HEAD + re-review) OPEN. Per-attempt authz hot path - session binding + CONSUMES the 88B1 resolver + CONSUMES the landed TIP-88B2 3-parameter consent resolver + dedicated principal-scoped idempotency + typed decision/evidence + revalidated-not-bearer permit + Option-B raw-class request semantics + concurrency. `docs/tips/tip_88b3_raw_export_authorization_decision/tip_88b3_planning_brief.md`.
- **TIP-88A-E2 - Policy Permit TTL Field**: **LANDED** (committed local `09c9359`; closeout `tip_88a_e2_closeout.md`; push pending). Additive schema extension adding an immutable per-`(PolicyId, PolicyVersion)` `PermitTtlSeconds` to the landed 88A policy catalog + the app-config bounds `[60,900]`-default / abs-max `3600` that validate it. Real migration (migration `20260721070118`; landed 88A migration byte-unchanged). `docs/tips/tip_88a_e2_policy_permit_ttl_field/`. Closed TIP-88B3 gate-3.
- **TIP-88B1-E1 - Fulfillment Expiry Projection**: **LANDED + PUSHED** (commit `ccc2e37`; closeout `4b25e2f`). Additive-only extension surfacing `RawExportFulfillmentRef.ValidUntilUtc` on the landed B1 resolver (the value it already reads but drops). No decision/schema/migration change. `docs/tips/tip_88b1e1_fulfillment_expiry_projection/`. Closed TIP-88B3 gate-2.
- **Raw-assembly / RawBioPackage slice** (LATER, **after TIP-88B3**): the actual raw read/assemble + real package/manifest hash + delivery + the one-time permit-CONSUMPTION ledger (owned here, NOT in TIP-88B3). NOT any of the above.

**Canonical sequence:** TIP-88A (landed `e63cdf9`) -> TIP-88B1 (landed `2c684cf`) -> TIP-88B2 Subject Export Consent (landed `8cd52a3`) -> **TIP-88B1-E1 Fulfillment Expiry Projection** (additive extension to landed B1; LANDED `ccc2e37`) -> **TIP-88A-E2 Policy Permit TTL Field** (additive schema extension to landed 88A; LANDED `09c9359`, push pending) -> **TIP-88B3 Raw Export Authorization Decision + Permit** (authorization/evidence/permit; gates 1-3 CLOSED, gate-4 (rewrite-from-landed-HEAD) OPEN — NOT READY_FOR_BUILD) -> raw assembly / delivery / consumption.

Do not build from this file.
