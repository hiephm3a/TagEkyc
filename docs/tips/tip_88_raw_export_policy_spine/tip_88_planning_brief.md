# TIP-88 (SUPERSEDED - split 2026-07-10, 88B further split 2026-07-12; 88B2 renamed -> 88B2/88B3 clean sequence 2026-07-19)

This combined "policy-gate foundation" brief was split for adversarial-verifiability. Current shape (governance index):

- **TIP-88A - Raw Export Policy Catalog Foundation** (INERT data spine; no runtime export capability): `docs/tips/tip_88a_raw_export_policy_catalog/`. **LANDED + PUSHED** (commit `e63cdf9`, closeout `62fed65`) - the 6-table append-only catalog.
- **TIP-88B - Raw Export Enforcement Spine** was itself SPLIT into two slices (too large as one): see `docs/tips/tip_88b_raw_export_enforcement_spine/tip_88b_planning_brief.md` (superseded pointer). The current pieces are:
  - **TIP-88B1 - Raw Export Control Plane**: identity surface + exact-version grant(+revoke) + fulfillment(+scoped recorder authority) + revocable runtime lifecycle + fail-closed activation gate + lock-capable effective-state resolver. Append-only, DB-enforced. **LANDED + PUSHED** (commit `2c684cf`; docs closeout `075bdcd` local). **NOT authorization/evidence/permit/package - those are TIP-88B3.** `docs/tips/tip_88b1_raw_export_control_plane/`.
  - The former **TIP-88B2 - Raw Export Authorization Decision** was SPLIT (review round-1) - subject consent is greenfield + a mandatory pre-authorize gate, so it is its own slice. Renamed 2026-07-19 to a clean sequential order (was awkward `88B2a + 88B2`):
    - **TIP-88B2 - Subject Export Consent** (**READY_FOR_BUILD v1.0**, flipped 2026-07-19; asymmetric session gates - `Granted` requires `Completed`, `Withdrawn` requires session-exists only; split per-event direct-insert invariants; pinned resolver lock contract): greenfield append-only subject consent (scope = session + subjectRef + policy + version + SERVER-DERIVED purpose + SERVER-DERIVED recipient) + dedicated recording/withdraw authority (deployment-only grant/revoke) + latest-event-overall/no-fallback on BOTH the consent and authority aggregates + TargetRevision + pinned event row-shapes + full DB privilege/SECURITY-DEFINER manifest + server-computed ConsentScopeHash with ONE pinned single-bigint advisory-lock mapping + golden vectors + a 3-parameter resolver TIP-88B3 consumes. RECORDS/RESOLVES the consented raw-class set only; class-coverage + denial causes are TIP-88B3's. **Review + land NEXT.** `docs/tips/tip_88b2_subject_export_consent/tip_88b2_planning_brief.md`.
    - **TIP-88B3 - Raw Export Authorization Decision + Permit** (BLOCKED_ON_TIP_88B2_LANDED): per-attempt authz hot path - session binding + CONSUMES 88B1 resolver + CONSUMES TIP-88B2 consent + dedicated principal-scoped idempotency + typed decision/evidence + revalidated-not-bearer permit + Option-B raw-class request semantics + concurrency. Rewritten from the landed TIP-88B2 impl; opens after TIP-88B2 lands. `docs/tips/tip_88b3_raw_export_authorization_decision/tip_88b3_planning_brief.md`.
- **Raw-assembly / RawBioPackage slice** (LATER, **after TIP-88B3**): the actual raw read/assemble + real package/manifest hash + delivery + the one-time permit-CONSUMPTION ledger (owned here, NOT in TIP-88B3). NOT any of the above.

**Canonical sequence:** TIP-88A (landed) -> TIP-88B1 (landed) -> **TIP-88B2 Subject Export Consent** (next to review+land) -> **TIP-88B3 Raw Export Authorization Decision + Permit** (authorization/evidence/permit; blocked on 88B2) -> raw assembly / delivery / consumption.

Do not build from this file.
